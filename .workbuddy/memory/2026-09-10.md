
## 代码审查（src/ 全量）
- 输出报告：docs/code-review-2026-09-10.md
- 关键发现：M1 访客 playerId 客户端可控可污染天梯（accounts.ts:418-433）；M2 断线重连不 rejoin + 服务端 sweep 删房导致丢局（useFriendRoom.ts:274 + room-socket.ts:774）；M3 AI rootCandidates 截断漏判必胜/必挡手（ai.ts:291,307,314）。
- 架构债：rooms.ts/useFriendRoom/GameShell/OnlineLobbyView 均为上帝文件，建议按报告顺序拆分。

## 修复 M1 + M2（commit 7be68a2，已推送 main）
- **M1 已核实并修复**：访客 playerId 原先由客户端生成并直接落库。现改为服务端签发 `guest_<random>`（accounts.ts `createGuestPlayerId`），`GuestSessionStore.createSession` 拒绝 `acct` 前缀形状的 id；客户端传的 playerId 仅作展示名。测试：accounts.test.ts 新增冒充/保留前缀两个用例。
- **M2 已核实并修复**（三层）：
  1. 客户端 useFriendRoom：socket.io 重连成功后（非首次 connect）自动用 storedSession 重发 `room:rejoin`；`room-not-found` 时清理本地房间态；
  2. rooms.ts `markDisconnected`：对局中全员断线不再立即 abandon+删房，保留至 disconnectGraceMs（默认 60s）宽限；`shouldDeleteRoom` 对应放宽为"全部 deadline 过期才可删"；`sweepExpiredRooms` 在删房前补 captureFinishedGame 保住 abandoned 战绩；
  3. room-socket.ts sweep：adapter 空房间中 status !== "waiting" 的获得 emptyRoomGraceMs（默认 60s）宽限（新 RegisterRoomSocketOptions：emptyRoomGraceMs/now）。
- 测试更新：rooms.test.ts 原"全员断线立即关闭房间"用例改为"宽限期内可重连 + 期限到后废弃删除"；room-socket.test.ts 新增宽限期回归测试、game-record 断言改用服务端签发 id。全量 174 测试通过。
- **环境坑（重要）**：本机 vitest 4.1.9 在小写盘符 cwd（`d:/...`）下所有测试报 `Cannot read properties of undefined (reading 'config')`（vitest#10692 盘符大小写双实例问题）；必须 `cd "D:/OneDrive/AiPrograms/gomoku-online"` 大写盘符再跑。另：tsc --noEmit 基线遗留 8 条错误（game-record-export/game-record-opening-analysis/room-socket 测试 helper 类型），与业务代码无关。

## 修复 M3 + M4/M5/m6/m7（commit d69d0bd、f30e273；评审文档入库 2267083；已推送 main）
- **M3**：`ai.ts` 必胜/必挡检测从「截断后的 candidates」改为全量 `candidatePool`。复核更正：原报告「五连点会被 rootCandidates 截断丢弃」**不成立**——`rankCandidateMoves` 是 tier 优先排序，`attack.wins/defense.wins` 分别锁 tier 0/1，必然落在 top-N 内。改动价值是**解耦**（战术判定不再依赖排序/截断/分片）。`ai.ts:969` 的二次切片是无害冗余（输入已是截断集），未动。测试补了拥挤局面必胜/必挡 + 分片一致性 + 随机密集局面不变量。
- **M5（关键坑）**：本仓 eslint 开了 `react-hooks/set-state-in-effect`（error），**禁止在 effect 里同步 setState**，所以评审报告建议的「挂载后 useEffect 恢复」会直接 lint 失败。改用 `useSyncExternalStore`：
  - 新增 `src/components/client-boot-state.ts`：`useBootGameMode()`（有 `?room=` → "room"）+ 空订阅 `subscribeToBootState`；
  - 模式：`useState<GameMode|null>(null)` 存覆盖值，`mode = modeOverride ?? bootMode`；
  - `useFriendRoom`：`accountStatus/playerName/joinTarget/isJoiningRoom` 同样「覆盖值 ?? 启动快照」，setter 名字不变（只写覆盖值）；服务端快照固定 `guest / "Player" / "" / false`，与旧 SSR 输出一致。
  - **约定**：以后本仓任何「首屏依赖 URL/storage」的需求都用这个 override ?? snapshot 模式，不要再在 mount effect 里 setState。
- **M4**：聊天发送加 ref 在途闸门（state 只驱动按钮禁用），乐观清空输入框、失败按需回填；`TableRoomChat`/`OnlineLobbyView` 加 `isSendingChat/isSendingPublicChat`。
- **m6/m7**：`pendingTransition` 期间锁定模式 pill（三处重复 disabled 条件抽成 `isModeSwitchLocked`）+ `handleModeChange` 直接忽略新点击；`leaveRoom` 加 8s 超时兜底、无房时也回调，保证 `isTransitioning` 不会永久 true。
- 验证：vitest 178 passed；tsc 仍 8 条基线错误；改动文件 eslint 干净。
- **未能验证**：真实浏览器 hydration 冒烟。sandbox 里 `npm run dev` 起不来、本机端口 curl（127.0.0.1:3000/3030/3210）全 000、`npm run build` 被工作机 safe-delete 批量删除保护拦在 `.next/trace`。需人工跑一次 `npm run dev` 看 console。
- **待办**：M6（持久化无界增长）、M7（XFF 可伪造绕过限流，需先确认线上是否在可信反代后，否则会误伤现有限流）、m10/m11 及其余 Minor/Nit。

## 复审最新修复提交（报告 docs/code-review-2026-09-10-followup.md，commit 675e990 已推送）
- 范围 7be68a2 / d69d0bd / f30e273。vitest 178 passed 复现成功；tsc/eslint 未复跑；仍无浏览器冒烟。
- 确认 M1/M2/M3 修复正确（M3 的解耦结论成立）；M5 用 useSyncExternalStore 方向正确，渲染期已无 window 副作用。
- **新发现 R1（Major，M5 副作用）**：client-boot-state.ts 的 `readGameModeFromUrl` 是唯一**没有 `??=` 缓存**的启动快照，等于每次渲染重读 URL（React 19 `updateSyncExternalStore` 非水合路径会用新值渲染并置 didReceiveUpdate）。`leaveRoom` 成功 / `clearClosedRoom` 都调 `clearRoomUrl()` 抹掉 `?room=` → mode 静默 room→local，用户离开房间后被丢到本地棋盘（`deriveGameWorkspace` 返回 "local"），与 `pendingTransition.nextMode = null`（「留在联机工作区」）语义冲突。
- **新发现 R2（Major，M4 修法反面）**：聊天闸门 `chatSendInFlightRef` 无 ack 超时。socket.io-client 4.8.3 `_clearAcks()` 只回调带 error 的 ack（`.timeout()` 产物），普通 emit 的 ack 断线时被**静默丢弃** → 发送按钮永久禁用且重连不恢复。修法：`.timeout(ms).emit(ev, payload, (err, res) => ...)`，注意签名与 RoomSocket 类型要同步放宽。
- R3~R7：英文超时文案未进 dictionary；超时后迟到 ack 仍执行清理与 `left=false` 冲突；8s 计时器无卸载清理；启动快照缓存是模块级 `let`（跨客户端路由不重置、测试不隔离）；新代码零测试。
- 顺带发现 `.gitignore` 未覆盖 `.workbuddy/`、`.codex/`、`.codex-remote-attachments/`、`tsconfig.tsbuildinfo`。
- 未改动任何业务代码（纯评审）；`docs/HANDOFF.md` 的未提交改动按约定未纳入本次提交。

## 修复 R1 + R2（commit e14ec7e；gitignore d00dd64；文档 3c448d4/f753020；已推送 main）
- **R1**：`client-boot-state.ts` 的 `readGameModeFromUrl` 补 `bootGameModeCache ??=`，启动快照只读一次 → `clearRoomUrl()` 后 mode 不再从 room 掉回 local。**没有**额外加 `setMode("room")`（快照定住后 `nextMode: null` 语义自然成立）。
- **R2**：新增 `src/components/chat-send-gate.ts`（`createChatSendGate`：`begin(onTimeout)` 同步判重入 + 8s 看门狗，`settle()` 幂等复位），房间聊天与公聊共用；超时回调复位按钮 + 回填输入框 + `CHAT_ACK_TIMEOUT_ERROR`（英文，hook 拿不到 dictionary，属 R3 遗留）。新增 `chat-send-gate.test.ts` 4 条（用 `vi.useFakeTimers()`）。
- 验证：`npx vitest run` **18 文件 / 182 测试全通过**；改动文件 `npx eslint` 退出码 0 无输出；`npx tsc --noEmit` 恰好 8 条既有基线错误（分布未变），无新增。
- **`.gitignore` 补 4 条**：`.codex/`、`.codex-remote-attachments/`、`.workbuddy/`、`tsconfig.tsbuildinfo` → `git status` 从此干净（工作区只剩 `M docs/HANDOFF.md`，已随 f753020 提交）。
- `docs/HANDOFF.md` 的 IX-07 步骤 1 记录已作为独立 docs 提交（内容只有重读与设计结论，IX-07 实现未开始）。
- 剩余：R3（hook 的英文错误文案需 locale 注入重构）、R4（超时后迟到 ack 与 left=false 冲突）、R5（计时器无卸载清理）、M6/M7/m10/m11 等。

## 未提交项收尾 + 技能经验沉淀
- 工作区确认干净：`docs/HANDOFF.md` 的 IX-07 记录已随 f753020 提交；`.codex/`（1.9M 证据截图）、`.codex-remote-attachments/`、`tsconfig.tsbuildinfo` 继续忽略（HANDOFF 自己写明证据目录不进库）。
- **`.workbuddy/memory/` 纳入版本库**：`.gitignore` 由 `.workbuddy/` 改为 `.workbuddy/*` + `!.workbuddy/memory/`（父目录未被排除才能反悔放行子目录）。这样项目记忆（vitest 盘符坑、sandbox 限制、RoomStore 语义、boot 快照约定、socket.io ack 结论）随仓库跨机器同步；无敏感串。
- 技能 `code-review-analyzer`（用户级 `~/.workbuddy/skills/`，marketplace 来源）升到 **1.2.5**：第四步新增「#### 依赖源码交叉验证（框架 / 库相关结论必做）」；质量标准加「依赖行为可溯源」；禁止行为加「仅凭记忆断言 Critical/Major」；参考文件登记新文档。新增 `references/dependency-source-verification.md`（触发判据、6 步操作、React 19 `updateSyncExternalStore` 重读未缓存快照 + socket.io-client 4.8.3 `_clearAcks` 丢弃普通 ack 两个实战案例、常见坑）。`_skillhub_meta.json` 置 `userModified: true` 防自动更新覆盖。
- 技能作者是 WXY-Dev（非 agent 生成），故**未**加 `agent_created`，走官方 userModified 流程。

## 复核 Codex 的 R3/R4/R5/R7 修复（修复提交 5056016；复核记录 87b457d 已推送）
- 用户说的「我已修复」= **Codex**（提交作者 `Codex <codex@local>`，15:51）做的修复：`leave-room-attempt.ts`（新增）、`client-boot-state.ts` 改成 `createBootGameModeReader()` 工厂、hook 加 `messages` 注入、`dictionaries.ts` 补六语种两条文案、卸载 effect 里 settle 三个闸门。
- 复核手法：**单模块变异测试**（把修复逐条改回坏的样子，新测试必须失败）。三处全部按预期失败 → 无空转断言：`cache ??=`→`cache =`（1/3 失败，`expected 'local' to be 'room'`）、聊天闸门去掉看门狗（2/4）、`settle()` 去掉 pending 守卫（2/3）。
- 配套复现：vitest **20 文件 / 188 通过**；tsc 恰好 8 条基线错误（1+1+6，分布未变）；改动 8 文件 eslint 退出码 0。
- 结论：**R1/R2/R3/R4/R5 修法成立，无假修复无新回归**。R4 的 ack 回调同时校验 `leaveRoomRequestRef.current === request` 与 `settle()`，迟到 ack 不会毒害下一次请求。
- 新增待办（已写进复审文档）：**R8** `useFriendRoom.ts:464`、`:516` 与 `formatConnectionError()`（:1700-1707）仍是英文串且同样进 `room-error`；**R9** R4 忽略迟到 ack 的取舍 → 若只是 ack 丢了而服务端已处理，本地会滞留「仍在房间」到用户重试（重试自愈）。**R6 实为部分保留**：boot mode 已是实例闭包，`useFriendRoom` 里四个 `boot*Cache`（:1446-1483）仍模块级。
- 踩坑记录：**`.tmp/` 并未被 .gitignore 覆盖**（只匹配了里面的 `*.log`），往里放 `.mjs` 会让 `git status` 出现 `?? .tmp/`；变异脚本用完即删。
