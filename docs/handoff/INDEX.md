# 交接文档索引 (Handoff Index)

> 本目录存放各阶段交付、重构审查与重要缺陷修复的离散交接文档（Handoff Artifacts）。
> 新任务交付时，请在顶部按倒序追加登记。

---

## 历史档案全集（零信息丢失归档）

- 📦 **[原始全量交接档案库 (5152 行)](../archive/LEGACY_HANDOFF_ARCHIVE.md)**  
  *涵盖 2026-06 至 2026-09-10 期间的全部历史交接记录（Stage 0 重做、Stage 1~3 进展、IX-00 至 IX-07 交互重构全部过程推导与会话沉淀，1:1 完整保留）。*

---

## 阶段交付文档列表（倒序排列）

| 交付日期 | 对应提交 / 变更主题 | 文档链接 | 核心交付成果 |
| **2026-09-23** | `fb17609` 独立审查 Round 2 复查（联机对战命名规范、排行榜搜索框修复、账号登录与防冒名技术设计方案） | [`2026-09-23-workbuddy-code-review-round2-handoff.md`](2026-09-23-workbuddy-code-review-round2-handoff.md) | 复查全量 `b2c6383..fb17609`（6 文件，+875/−0，纯文档）；**判定未通过**：2×P1 / 1×P2 / 3×P3，无 P0。**P1-1**：路径 A（纯令牌登录）前置短路（方案 `:229-237`）⇒ 认领分支处 `input.token` 必为空、`isOwner` 恒假 ⇒ 恒返回 `account-password-required`，"凭原令牌绑定密码"为**死代码**，Round 1 验收的正向用例不可能成立。**P1-2**：`loginAccount` 混用快照与活对象——`findByPublicHandle`（`accounts.ts:212-216`）/ 方案 `findByDisplayName`（`:193-199`）返回 `AccountSnapshot`（`accounts.ts:601-613` 无 `passwordHash`/`tokenHashes`）⇒ 按昵称/代号登录恒落入认领分支、密码比对不可达；字面实现即 `tsc` 红（探针 `TS2339`×2），绕过类型后 `tokenHashes.filter` 抛 `TypeError`、`persist(快照)` 写入缺字段行 ⇒ 重载后密码与会话被抹除。**P2-1**：`room.nameReservedError` 六语种文案无上屏通道——服务端 `ack()` 后紧跟 `socket.emit("room:error")`（`room-socket.ts:1016-1035`），客户端 `room:error` 监听器（`useRoomSocket.ts:183-185`）覆盖字典文案，独立 Socket.IO 探针实测派发顺序恒为 `ack → event`，全仓无错误码→字典映射层。**P3-1**：`RoomErrorCode`（`room-state-machine.ts:166-201`，35 成员封闭联合）未同步新增 4 码 ⇒ 探针实测 `TS2322` 实现即门禁 1 红；Round 1 明示要求的 HTTP 状态码映射（401/409/429）在方案中零处出现。**P3-2**：`tokenHashes` 淘汰语义（文档"最近活跃"vs 伪代码按签发序 `slice(0,5)`、`authenticate` 不回写次序）不一致 ⇒ 长期活跃旧设备被静默登出。**P3-3**：`ScryptConcurrencyGate` 队列无上界/无超时；并发越界反例尝试**未复现**（微任务 FIFO），故仅主张背压契约缺失。**通过项**：Round 1 四项实质闭环——P2-2 异步 scrypt + 2 并发门禁成立；P2-3 `canonicalizePlayerName` 对枚举的 4 类变体全部拦截（含数学粗体，探针实测）；P2-4 多会话结构自洽；P2-1 错误码解耦成立（客户端自愈为显式白名单）。基线 `tsc` 0 错误，探针用后即删、工作区干净 |
| **2026-09-23** | 联机对战命名规范、排行榜搜索框修复、账号登录与防冒名设计方案 Round 1 审查缺陷闭环（8项缺陷全闭环，待审交付） | [`2026-09-23-online-pvp-auth-and-leaderboard-round1-remediation-handoff.md`](2026-09-23-online-pvp-auth-and-leaderboard-round1-remediation-handoff.md) | 针对 WorkBuddy Round 1 审查报告指出的 8 项缺陷（1×P1 / 4×P2 / 3×P3）实施 100% 闭环修复：①P1-1 遗留账号认领增加原设备 Token 所有权凭据强校验，拒绝无凭据接管；②P2-1 引入独立错误码 name-reserved，解除与房内同名自愈逻辑耦合，确保提示上屏；③P2-2 改用异步 crypto.scrypt 并在 libuv 工作线程池调度，增设 2 并发门禁与 10次/分 IP 限流，主事件循环停顿 0.00ms；④P2-3 落地 canonicalizePlayerName（NFKC + 剥离 \p{Cf} 零宽隐形字符 + 小写），全链路统一口径彻底封堵变体绕过；⑤P2-4 扩充 tokenHashes 数组支持 5 个多会话令牌共存，消除与备份承诺冲突；⑥P3-1 补齐 findByDisplayName、错误码联合与令牌回填；⑦P3-2 三态 Pill 补齐 WAI-ARIA tab 契约与方向键 roving focus；⑧P3-3 修复 svg 颜色穿透与 :focus-within 高亮轮廓；本地四道门禁全绿 |
| **2026-09-23** | `0c792ff` 独立审查 Round 1（联机对战命名规范、排行榜搜索框修复、账号登录与防冒名技术设计方案） | [`2026-09-23-workbuddy-code-review-round1-handoff.md`](2026-09-23-workbuddy-code-review-round1-handoff.md) | 审查全量 `b2c6383..0c792ff`（4 文件，+474/−0，纯文档）；**判定未通过**：1×P1 / 4×P2 / 3×P3，无 P0。**P1-1**：旧账号"无密码认领"分支无任何所有权证明 ⇒ 凭公开昵称即可接管全部历史无密码账号（方案自述 80+ 条），且登录即轮换唯一 `tokenHash`（`accounts.ts:60/190`）+ 现有路由无找回入口（`online-server.ts:66`）⇒ 原主永久失去账号；与 1.4 节守门保证直接矛盾。**P2-1**：守门复用 `duplicate-name`，与"房内同名"既有语义（`room-state-machine.ts:460`）及客户端自愈路径（`useRoomSocket.ts:329/356-375`、`395/419-437` 默认 `retryWithFreshIdentity=true` 静默改名重试）冲突 ⇒ §2.4.2 承诺的 `nameReservedError` 提示在 join 两条主路径上是死文案。**P2-2**：`scryptSync` 同步阻塞单进程事件循环，实测 p50 40.7ms（方案自称 10~20ms），单 IP 一窗口 10 次 ≈400ms 阻塞 ⇒ "完全不增加对局事件循环负担"被证伪。**P2-3**：守门谓词无名称规范化契约，零宽空格/word joiner/软连字符等不可见字符可绕过（真实模块探针实测 `isNameReserved=false` 且 `resolvePlayerIdentity` 返回 `playerName:"Alice"`）⇒ "彻底斩断冒名通道"不成立。**P2-4**：单 `tokenHash` + 登录轮换与 §1.3.5/§2.6.2"令牌导出备份/跨浏览器转移"目标互斥。**P3-1**：`account-not-found`/`invalid-password` 不在 `AccountError.code` 封闭联合、`findByDisplayName` 不存在 ⇒ 按字面实现必触 `tsc` 红。**P3-2**：三态 Pill 缺 ARIA/键盘/焦点契约（项目已有 `TableSidebarTabs.tsx:34-64` 范式）。**P3-3**：搜索框 hover 规则被 `globals.css:975-979` 的 svg `color` 声明架空、`outline: none` 无替代焦点态。通过项：四项需求落点与真实代码耦合点一致、门禁位置（`resolvePlayerIdentity` 单点）选型正确、JSONL 新字段向下兼容、6 语种 12 处旧文案与 `dictionaries.test.ts` 结构守门均核实 |
| **2026-09-23** | 联机对战命名规范、排行榜搜索框修复、账号登录与防冒名技术设计方案（待审交付） | [`2026-09-23-online-pvp-auth-and-leaderboard-plan-handoff.md`](2026-09-23-online-pvp-auth-and-leaderboard-plan-handoff.md) | 针对用户反馈的四大核心问题制定端到端技术架构与交互方案：①在 6 语种中将“好友房”全面演进为“联机对战”（Online PVP）；②排行榜搜索框限宽 280px，彻底重置内嵌按钮样式至 20px 嵌入态，消除悬浮重叠；③引入原生 crypto 加盐与 scryptSync 密码哈希模型，升级 AccountStore 并新增 POST /api/account/login 端点（10次/分限流），支持旧账号认领与 Token 导出备份；④在 AccountStore 开放 isNameReserved 并由 resolvePlayerIdentity 强制守门，彻底封堵访客冒用已注册昵称漏洞；本地四道门禁全绿 |
| **2026-09-23** | `72eb0ac` 独立审查 Round 7 复查（反馈入口按钮文字标签 + Windows 文件重写锁容错） | **审查通过 (PASS, 0×P0~P3)** | 复查全量 `f5d33ae..72eb0ac`（24 文件，+1482/−50）与增量 `bf895b0..72eb0ac`；**判定通过（PASS，0×P0/P1/P2/P3）**。①Round 6 P3-1 守门单测经独立逐站点变异实测 3/3 独立变红（AccountStore、GameRecordStore、GuestSessionStore 三处兜底守门完备）；②外部 holder 探针独立证实 16/20/30/40ms 档位 5/5 恢复，持续锁安全失败且旧文件逐字节不损、零 temp 残留，CPU 0.16% 杜绝忙等；③6 组变异探针全部变红；④高负载 8 路超订 4/4 全绿，空闲 11/11 全绿（35 套/339 例），四道门禁与 smoke:persistence 5/5 全通过；⑤6 语种文字标签渲染正常，满足 WCAG 2.5.3 与 RTL 镜像排版。双智能体审查闭环圆满完成 |
| **2026-09-23** | 反馈入口按钮文字标签与文件重写锁容错 Round 6 审查缺陷修复（1×P3 全闭环，待审交付） | [`2026-09-23-feedback-button-label-round6-remediation-handoff.md`](2026-09-23-feedback-button-label-round6-remediation-handoff.md) | 针对 WorkBuddy Round 6 源码审查报告（`bf895b0`）指出的缺陷（1×P3）实施 100% 闭环修复：①P3-1 为全部三处 `compactFile` 异常捕获兜底补齐独立守门单测，在 `accounts.test.ts` 补齐 `AccountStore` 守门用例、在 `game-records.test.ts` 补齐 `GameRecordStore` 守门用例，经独立变异探针实测（三处 catch 各自改 throw）**3/3 独立、确定性变红**；②落实非阻断优化：放宽 `jsonl-file.test.ts:192` 墙钟断言至 `< 2000ms`（依赖 5 条确定性 `waitSpy` 断言守门），为 `accounts.test.ts:130` 显式配置 15s 超时，并在 `jsonl-file.ts` docstring 标注实测外部探针采样口径，彻底消除 CPU 饱和与超订多 worker 下的 flake；本地四道门禁全绿（35 套 / 339 项单测全绿，Next.js 生产构建 18/18 页面通过，smoke:persistence 5/5 通过） |
| **2026-09-23** | `cb347c8` 独立审查 Round 6 复查（反馈入口按钮文字标签 + Windows 文件重写锁容错） | [`2026-09-23-workbuddy-code-review-round6-handoff.md`](2026-09-23-workbuddy-code-review-round6-handoff.md) | 复查全量 `f5d33ae..cb347c8`（21 文件，+1208/−48）与本轮增量 `81bc3c7..cb347c8`（`src/` 4 文件 +60/−11）；**判定未通过**：0×P0/P1/P2，**1×P3**。**P3-1**：Round 5 P3-2 仅 1/3 闭环 —— Round 5 明文枚举的三处 `compactFile` 兜底中仅 `GuestSessionStore`（`accounts.ts:501`，由 `accounts.test.ts:538` 覆盖）被守门；变异 `AccountStore`（`accounts.ts:332`）→ 全量 **35 套/337 例全绿 exit=0**；变异 `GameRecordStore`（`game-records.ts:489`）→ **35 套/337 例全绿 exit=0**；对照变异 `GuestSessionStore` → `accounts.test.ts` **1 failed/17 passed**（探针手法有效）。修复建议：比照现有范式各补一条同构用例并**分别**自证变红。通过项：Round 5 P3-1 **文档口径真闭环** —— `jsonl-file.ts:73-85` 删除 `bounded to ~45ms`，改为「空闲 n=150 分位数（p50 ~47 / p90 ~50 / max ~95ms）+ 饱和尾部无界（n=360，max ~1.0s）」双口径，与 STATUS/INDEX 逐项一致；**验收标准 4 经独立外部 holder 探针证实** —— 真实 `renameSync` + 外部持锁子进程精确释放实测 `16/20/30/40ms → 5/5`、`45ms → 4/5`、`50ms → 2/5`、`55/60ms → 0/5`（窗口 ≈45~50ms），持续持锁规范抛 `EPERM`（停顿 63ms）、旧文件逐字节不变、`.compact.tmp` 零残留、释放后 7ms 内自愈落盘；6 语种 `navLabel` 齐全、`aria-label` 与可见文本同源同值、新增 CSS 零硬编码 left/right（该三文件本轮未改动）；`tsc` 0 错误 / `lint` 0 错误 0 警告。另记 2 项待确认风险（`jsonl-file.test.ts:192` `<300ms` 墙钟断言本轮未修改，独立实测其操作数在 6 核 8 路超订下 `n=300` `min 44/p50 141/p90 224/p99 298/max 632ms`、`≥300ms` 3/300=1.0% ⇒ 验收标准 5「彻底消除高负载 flake」未获代码动作背书；首轮 10 次空闲全量运行出现 1 红，8 路超订首轮红于 `accounts.test.ts:130` `Test timed out in 5000ms`，属既存用例负载敏感性而非本轮回归）与 1 项未复跑项（`npm run build` / `smoke:persistence`） |
| **2026-09-23** | 反馈入口按钮文字标签与文件重写锁容错 Round 5 审查缺陷修复（2×P3 全闭环，待审交付） | [`2026-09-23-feedback-button-label-round5-remediation-handoff.md`](2026-09-23-feedback-button-label-round5-remediation-handoff.md) | 针对 WorkBuddy Round 5 源码审查报告（`0596ec1`）指出的缺陷（2×P3）实施 100% 闭环修复：①P3-1 彻底移除 `jsonl-file.ts:80` docstring 中的 `bounded to ~45ms` 绝对化断言，客观对齐实测经验分位数分布（空闲 n=150: p50 ~47ms, p90 ~50ms, max ~95ms；CPU 饱和 n=360 尾部因调度抢占无界可达 ~1.0s），与 STATUS 事实基准完全统一；②P3-2 在 `AccountStore`、`GuestSessionStore`、`GameRecordStore` 的 `compactFile()` catch 块中增加运行时诊断日志（`console.warn`），并在 `accounts.test.ts` 新增确定性守门单测，通过 `compactAfterLines: 1` + `openSync` 锁定实测压缩捕获，经变异探针验证（catch 改 throw）确定性变红；③回填 Round 4 修复交接单中的真实提交 SHA `81bc3c7`；本地四道门禁全绿（35 套 / 337 项单测全绿，Next.js 生产构建 18/18 页面通过，smoke:persistence 5/5 通过） |
| **2026-09-23** | `81bc3c7` 独立审查 Round 5 复查（反馈入口按钮文字标签 + Windows 文件重写锁容错） | [`2026-09-23-workbuddy-code-review-round5-handoff.md`](2026-09-23-workbuddy-code-review-round5-handoff.md) | 复查全量 `f5d33ae..81bc3c7`（19 文件，+993/−47）与本轮增量 `d2e9bcb..81bc3c7`（`src/` 4 文件 +83/−24）；**判定未通过**：0×P0/P1/P2，**2×P3**。**P3-1**：Round 4 P3-1 第③项验收未闭环 —— `jsonl-file.ts:80` 仍写 `keeping persistent-lock stalls bounded to ~45ms`（绝对化上界），实测空闲 `n=150` `min 41.85/p50 46.66/p90 49.73/p99 74.09/max 94.51ms`（尾部 2.1x）、6 核 8 路超订 `n=360` `p50 140.6/p90 222.3/p99 657.8/max 1034.8ms`（>300ms 22/360=6.1%，越界 23x），且 docstring 未附 n/采样口径，与 `STATUS.md:35`「彻底消除绝对化断言」自相矛盾；对照组旧默认 `maxAttempts=2` 同负载亦越界（3/120、max 588.5ms）⇒ 属同步 `Atomics.wait` 固有效应。**P3-2**：本轮新增三处 `compactFile` 兜底（`accounts.ts:331/501`、`game-records.ts:488`）零守门 —— 变异探针改回 rethrow 后全量 **35 套/336 例全绿 exit=0**（对照：默认 4→1 变红 3 例、删 unlink 变红 1 例、忙等替身变红 1 例，探针手法有效）；行为本身经独立端到端验证正确（持句柄 `openSync(dest,"r")` → `createAccount` 不抛、追加行不丢、无 `.compact.tmp`，`closeSync` 后 6.2ms 内自愈压平至 5 行）。通过项：Round 4 **P2-1 真闭环** —— 外部 holder busy-spin 精确释放实测 **16/18/20/24/30/40ms 六档全 5/5 恢复**（≥60ms 才 0/5），持续占用安全失败、旧文件逐字节不损、temp 零残留；裸 `Atomics.wait(5)`×200 CPU **0.00%**、注入式重试 ×150 CPU 0.45%；6 语种预渲染产物 `aria-label === .feedback-nav-label`、`ar` 为 RTL、`.feedback-nav-label` 零硬编码 left/right；`npm test` 连跑 **10 次 10/10 全绿**、tsc 0 / lint 0 错误 0 警告 / build 18/18 / `smoke:persistence` 干净端口 5/5。另记 2 项待确认风险（`jsonl-file.test.ts:192` `<300ms` 墙钟断言饱和越界 6.1%，非本轮回归；`appendJsonlLine` 在兜底之外 + HTTP 入口无兜底，本轮复现失败）与 2 项非阻断建议 |
| **2026-09-18** | 反馈入口按钮文字标签与文件重写锁容错 Round 4 审查缺陷修复（1×P2, 1×P3 全闭环，待审交付） | [`2026-09-18-feedback-button-label-round4-remediation-handoff.md`](2026-09-18-feedback-button-label-round4-remediation-handoff.md) | 针对 WorkBuddy Round 4 源码审查报告（`68213dd`）指出的缺陷（1×P2, 1×P3）实施 100% 闭环修复：①P2-1 将 `atomicRenameSync` 的默认 `maxAttempts` 调优为 4，恢复窗口扩充至 ~45ms，经独立探针实测 20ms 与 30ms 释放档位均实现 5/5 完美恢复落盘；在 `AccountStore`、`GuestSessionStore`、`GameRecordStore` 的 `compactFile()` 中增加 `try/catch` 兜底，防止极端持久锁导致异常冒泡破坏业务接口；②P3-1 代码 docstring 与交接单全面对齐实测分位数（单次微休眠 ~15.1ms，持续锁停顿 ~45ms），彻底消除绝对化断言；③非阻断项：防御 `maxAttempts: NaN` 并在单测中覆盖；本地四道门禁全绿（35 套 / 336 项单测全绿，Next.js 生产构建 18/18 页面通过，smoke:persistence 干净端口 5/5 通过） |
| **2026-09-18** | `d2e9bcb` 独立审查 Round 4 复查（反馈入口按钮文字标签 + Windows 文件重写锁容错） | [`2026-09-18-feedback-button-label-workbuddy-code-review-round4-handoff.md`](2026-09-18-feedback-button-label-workbuddy-code-review-round4-handoff.md) | 复查全量 `f5d33ae..d2e9bcb`（15 文件，+785/−26）与本轮增量 `5562ecc..d2e9bcb`（`src/` 2 文件 +26/−15）；**判定未通过**：0×P0/P1，**1×P2、1×P3**。**P2-1**：默认 `maxAttempts` 3→2 在把停顿减半的同时把**瞬态锁恢复窗口腰斩** —— 注入 `renameFn` 实测尝试间隔 `p50 15.09 / p90 15.96 / max 17.84ms`（n=60），外部 holder busy-spin 精确释放实测 **16ms→1/5、18ms→1/5、20ms→0/5、24ms→0/5、30ms→0/5**，对照修复前默认 3 时 12~24ms 档全 5/5、30ms 档 4/5；Round 3 交接单所称「20ms 释放锁测试证实恢复语义未丢（`recovered: true`）」实测 **0/5** ⇒ 验收标准 4「瞬态锁可成功恢复」在文档引用场景不成立。根因：单次 `Atomics.wait(5)` 实测 `p50 15.18 / max 22.06ms`（n=40）⇒ 窗口 ≡ (maxAttempts−1)×~15.1ms，与停顿同源，同步语义下无法同时满足「停顿 <20ms」与「窗口 >20ms」；须改真异步退避（锁等待移出请求线程、保留 `.compact.tmp` 重排），或明确取舍并改写验收线。**P3-1**：docstring `bounding event-loop stall to < 20ms on idle systems` 与 STATUS/INDEX/round3 交接单「严格达成 < 20ms 验收红线」与放宽采样实测不符 —— 空闲 n=120 三轮 `p50 15.44/p90 16.55/max 30.9（>20ms 4.2%）`、`p90 25.12/max 40.36（12.5%）`、`p90 26.44/max 145.61（16.7%）`，6 路 CPU 饱和 n=40 `p50 133.4/p90 549.0/max 1337.9ms`。通过项：新构建产物 6 语种 `<svg>`+`.feedback-nav-label` 同渲染、`aria-label === 可见文本`（WCAG 2.5.3 全达标）、`ar` 为 `<html dir="rtl">`、零硬编码 left/right；`npm test` 连跑 **3/3 全绿**（35 套）、`npm run build` 18/18、`smoke:persistence` 干净端口 3111 **5/5**；**5 组变异探针全部变红**；持续 EPERM 下 200 次调用墙钟 3099ms / CPU **0.00%**（忙等闭环）、无 `.compact.tmp` 残留、旧文件不被破坏。另记 1 项待确认风险（重试耗尽抛出路径 `online-server.ts:27` 无 try/catch 且全仓无 uncaughtException 兜底，触发域随窗口腰斩而扩大）与 2 项非阻断建议（`maxAttempts: NaN` 静默 no-op；round3 修复交接单 HEAD_SHA 写作孤儿提交 `68403a4`）|
| **2026-09-18** | 反馈入口按钮文字标签与文件重写锁容错 Round 3 审查缺陷修复（1×P3 全闭环，待审交付） | [`2026-09-18-feedback-button-label-round3-remediation-handoff.md`](2026-09-18-feedback-button-label-round3-remediation-handoff.md) | 针对 WorkBuddy Round 3 源码审查报告（`5562ecc`）指出的 1 项缺陷（1×P3）实施 100% 闭环修复：①P3-1 将 `atomicRenameSync` 的默认 `maxAttempts` 调优为 2（`const maxAttempts = Math.max(1, options?.maxAttempts ?? 2);`），持续锁下仅进行 1 次微休眠，空闲系统实测端到端同步停顿压缩至 ~11~16ms（严格达成 < 20ms 验收线），且 20ms 释放锁测试证实恢复语义未丢（`recovered: true`）；修正 docstring 指标并移除 `strictly bounding` 绝对化断言，注明实测值与负载相关性；单测同步对齐确定性断言（`attempts=2`, `waitSpy` 1 次 5ms），消除 flake 隐患；②文书建议：回填 Round 2 修复交接单的真实提交 SHA `0398bdc`；本地四道门禁全绿（35 套 / 335 项单测全绿，Next.js 生产构建 18/18 页面通过，smoke:persistence 干净端口 5/5 通过） |
| **2026-09-18** | `0398bdc` 独立审查 Round 3 复查（反馈入口按钮文字标签 + Windows 文件重写退避重试） | [`2026-09-18-feedback-button-label-workbuddy-code-review-round3-handoff.md`](2026-09-18-feedback-button-label-workbuddy-code-review-round3-handoff.md) | 复查全量 `f5d33ae..0398bdc`（13 文件）与增量 `57a73f1..0398bdc`（`src/` 4 文件 +30/−26）；**判定未通过**：0×P0/P1/P2，**1×P3**。**P3-1**：第 2 轮 P3-1 明示的「强制 EPERM 下事件循环最大停顿 < 20ms」验收线**仍未达成**（实测 `atomicRenameSync(maxAttempts=3)` 30.3ms、`rewriteJsonlFile` 端到端 33/35ms、200 次调用平均 30.77ms）；且本轮唯一功能改动 `sleep(5*attempt)→sleep(5)` 对真实停顿**零收益**（Windows 15.625ms 定时器粒度把 5ms/10ms 一并量化，请求预算 15→10ms 而真实停顿 30.5→30.77ms 未减，实质只改了注释与文档）；新写入 docstring 的 `strictly bounding ~30-36ms` 在 6 路 CPU 饱和下被证伪（40 采样 `p50 83/p90 136/max 170ms`）。修复建议：默认 `maxAttempts` 降为 2（实测 11.6ms 达线且 20ms 释放仍可恢复）或改异步退避，并同步修正 docstring。通过项：6 语种预渲染产物 `aria-label === 可见文本`（WCAG 2.5.3 全达标）、`ar` RTL、零硬编码 left/right，删除 zh 一条 `navLabel` 即 `tsc` exit=2 + 字典测试变红；`npm test` 连跑 **10 次 10/10 全绿**（35 套/335 例）并在 6 路 CPU 饱和下 4/4 全绿；**4 组变异探针全红**；真实 EPERM 端到端确认第 2 轮 P2-1 真闭环（20ms 释放恢复、持续占用安全抛错无 temp 残留、200 次调用 CPU 0.76%）；tsc 0 / lint 0 错误 0 警告 / build 18/18 / `smoke:persistence` 干净端口 5/5 |
| **2026-09-18** | 反馈入口按钮文字标签与文件重写锁容错 Round 2 审查缺陷修复（1×P2, 1×P3 全闭环，待审交付） | [`2026-09-18-feedback-button-label-round2-remediation-handoff.md`](2026-09-18-feedback-button-label-round2-remediation-handoff.md) | 针对 WorkBuddy Round 2 源码审查报告（`fa50c64`）指出的 2 项缺陷实施 100% 闭环修复：①P2-1 彻底移除 `jsonl-file.test.ts` 中的脆弱墙钟断言（`expect(elapsed).toBeLessThan(40)`），改用 `vi.spyOn(Atomics, "wait")` 确定性断言调用次数（2 次）与单次参数（5ms），宽放墙钟上限至 `< 300ms` 仅用于捕获忙等回归；批量合并 `accounts.test.ts` 中 185 次零散追加，杜绝高频 I/O 引发 Defender 扫描锁争用；在 `atomicRenameSync` 中增加 `Math.max(1, ...)` 防御并补齐用例；连续 10 次全量 `npm test` 实测 100% 全绿（10/10 PASS，0 flake），3 组变异探针全部变红；②P3-1 调整重试休眠为固定 5ms，将全仓交付文档与 docstring 的休眠与停顿指标修正为真实实测值（Windows 15.625ms 时钟中断量化下单次休眠 ~15.2ms、20ms 释放恢复 ~22~27ms、持续锁占用耗尽 ~30~36ms 规范抛 EPERM，0% CPU 忙等）；本地四道门禁全绿（35 套 / 335 项单测全绿，Next.js 生产构建 18/18 页面通过，smoke:persistence 干净端口 5/5 通过） |
| **2026-09-18** | `57a73f1` 独立审查 Round 2 复查（反馈入口按钮文字标签 + Windows 文件重写锁容错） | [`2026-09-18-feedback-button-label-workbuddy-code-review-round2-handoff.md`](2026-09-18-feedback-button-label-workbuddy-code-review-round2-handoff.md) | 复查全量 `f5d33ae..57a73f1`（10 文件，+417/−9）；**判定未通过**：0×P0/P1，**1×P2、1×P3**。**P2-1**：`jsonl-file.test.ts:187` 的墙钟断言 `expect(elapsed).toBeLessThan(40)` 在全量 `npm test`（35 套并行 worker 池）下不稳定变红 —— 连续 10 次实测 **2 红 / 8 绿**（红值 54ms、143ms；单跑该文件恒绿），根因是该重试路径真实代价 `p50=30.5ms` 与 40ms 上界余量不足 10ms（Windows 定时器粒度 15.625ms 使 `Atomics.wait(5)/(10)` 实睡 15.26/15.20ms，请求 15ms 实付 30.67ms），且 6 核 CPU 饱和下分布达 `p90=48.6/max=51.3ms` → 验收标准 6「四道门禁全绿」不可复现；须改为确定性断言（注入 `sleepFn` 断言 `[5,10]`+`attempts===3`），墙钟上界放宽至仅能捕获忙等量级。**P3-1**：实测持续 EPERM 下单次 `rewriteJsonlFile` **同步占用 34.86ms**（10ms 采样器零执行、30ms 定时器未触发），越过其自设「<20ms」验收线与文档声明「≤15ms」预算约 2 倍；`online-server.ts` 单进程托管 Next.js+Socket.IO 且 `persist()→compactFile()` 同步执行 ⇒ 一次持续锁即冻结全服 30~35ms → Round 1 P2-1 属**部分闭环**；建议固定单次 `sleep(5)` 或改异步退避，并同步修正 docstring/STATUS/INDEX 数值。通过项：生产预渲染产物 6 语种 `aria-label === 可见文本`（WCAG 2.5.3 全达标）、`navLabel` 6 语种齐全、零硬编码 left/right、`ar` 为 RTL；Round 1 P3-1/P3-2 经 3 组变异探针（白名单剔 EBUSY / 删 temp 清理 / `maxAttempts` 默认改 1 均变红）+ a11y 逐语种核验确属真闭环；重试语义独立端到端证实生效（外部进程 20ms 释放锁即成功落盘，持续占用则旧文件不损坏且 temp 已清理）；tsc 0 / lint 0 错误 0 警告 / build 18/18 全绿，`smoke:persistence` 干净端口 5/5 通过 |
| **2026-09-18** | 反馈入口按钮文字标签与文件重写锁容错 Round 1 审查缺陷修复（1×P2, 2×P3 全闭环，待审交付） | [`2026-09-18-feedback-button-label-round1-remediation-handoff.md`](2026-09-18-feedback-button-label-round1-remediation-handoff.md) | 针对 WorkBuddy Round 1 源码审查报告（`c83a0ea`）指出的 3 项缺陷实施 100% 闭环修复：①P2-1 彻底移除 `atomicRenameSync` 的 CPU 忙等循环，改用 Node.js 主线程支持的原生 `Atomics.wait(sleepBuffer, 0, 0, ms)`，重试上限设为 3 次、总睡眠预算严格控制在 ≤ 15ms（远低于 20ms 门限，绝不阻塞 Socket.IO 心跳与 HTTP 响应），错误码白名单严格收敛至 EPERM/EBUSY，并在 `rewriteJsonlFile` 失败时自动安全 unlink 清理 `.compact.tmp`；②P3-1 在 `jsonl-file.test.ts` 补齐 5 项针对性自动化测试（瞬态失败后重试成功、非瞬态 EACCES 快速失败、持续 EPERM 上限受控且延迟极低、失败临时文件清理），且经变异探针实测篡改 `maxAttempts=1` 时立刻变红；③P3-2 将 `GameShell.tsx` 反馈链接的 `aria-label` 改为 `feedbackDictionary.navLabel`，使 6 语种可访问名称完全等于可见文本，100% 满足 WCAG 2.5.3 (Label in Name)；本地四道门禁全绿（35 套 / 334 项单测全绿，Next.js 生产构建 18/18 页面通过） |
| **2026-09-18** | `d317764` 独立审查 Round 1（反馈入口按钮文字标签 + Windows 文件重写重试） | [`2026-09-18-feedback-button-label-workbuddy-code-review-round1-handoff.md`](2026-09-18-feedback-button-label-workbuddy-code-review-round1-handoff.md) | 审查全量 `f5d33ae..d317764`（7 文件，+105/−2）；**判定未通过**：0×P0/P1，**1×P2、2×P3**。**P2-1**：`jsonl-file.ts:61-76` 新增的 `atomicRenameSync` 用 `while (Date.now() - start < 15*attempt) {}` 同步忙等实现退避，实测单次 `rewriteJsonlFile` **同步占用 697ms**、期间 30ms 定时器零触发、10ms 采样器零执行（事件循环完全停摆）→ 单进程全栈服务（Next.js + Socket.IO）HTTP/WS 全面冻结，相对基线（单次 rename 快速失败）属明显延迟回归，且 CPU 100% 空转；须改非阻塞退避或压缩预算 + `Atomics.wait` 并收敛白名单为 EPERM/EBUSY。**P3-1**：本次 diff 未新增任何测试，把 `maxAttempts` 改为 1（等价删除重试）后 `jsonl-file.test.ts` 仍 5/5 全绿 → 验收标准 ④ 完全无守门。**P3-2**：`GameShell.tsx:493` 的 `aria-label={title}` 与新增可见文本 `navLabel` 不一致，实测 zh「意见反馈/用户反馈与建议」、fr、ru 三语种可访问名称不含可见文本，违反 WCAG 2.5.3 Label in Name（en/es/ar 恰为前缀故未暴露）。通过项：6 语种标签真实浏览器渲染、RTL 镜像正确、6 语种 × 2 视口 × 2 布局 24 组零横向溢出、字典三断言有效；四道门禁独立复跑全绿（tsc 0 / lint 0 错误 0 警告 / vitest 35 套 329 例 / build 18/18） |
| **2026-09-18** | 用户反馈入口按钮增加文字标签与 Windows 文件重写防锁强化（待审交付） | [`2026-09-18-feedback-button-label-handoff.md`](2026-09-18-feedback-button-label-handoff.md) | 依据用户反馈对顶部操作栏反馈按钮进行易用性增强：①在 `FeedbackDictionary` 类型中扩充 `navLabel: string;` 字段，并在 6 语种中补齐规范短文案（en: "Feedback", zh: "意见反馈", fr: "Commentaires", es: "Comentarios", ru: "Обратная связь", ar: "الملاحظات"）；②`GameShell.tsx` 顶部操作栏反馈链接内渲染文本标签，保留 A11y 与 title 属性；③`globals.css` 完善 `.feedback-nav-link` 布局样式（gap 6px、padding 0 12px、nowrap、18px 图标对齐），完全兼容 RTL 镜像；④`src/server/jsonl-file.ts` 引入 `atomicRenameSync` 针对 Windows 文件锁提供退避重试，杜绝 EPERM 偶发；四道门禁全绿（35 套 / 329 项单测全绿，Next.js 生产构建 18/18 页面通过） |
| **2026-09-18** | 用户反馈系统 (Feedback) Round 2 审查缺陷修复（2×P2, 1×P3 全闭环，待审交付） | [`2026-09-18-feedback-round2-remediation-handoff.md`](2026-09-18-feedback-round2-remediation-handoff.md) | 针对 WorkBuddy Round 2 源码审查报告（`8b3e165`）实施 100% 缺陷闭环：①P2-1 修复 ≥128 KiB 及 10 MiB 大报文 TCP RST 导致 413 不可观测缺陷，重构 `readFeedbackJsonBody` 在 `bytesRead > maxBytes` (64 KiB) 时停止累加但自然排空至 `end`（设 15 MiB 硬上限保护），在 `end` 事件中 reject 并返回规范 HTTP 413，移除抢跑 destroy，彻底消除 ECONNRESET；②P2-2 修复 chunk 边界切裂多字节 UTF-8 字符（CJK 3 字节、Emoji 4 字节）导致 `\uFFFD` 损坏落盘缺陷，恢复调用 `request.setEncoding("utf8")` 由 Node 内部 `StringDecoder` 跨 chunk 重组多字节字符，落盘正文 100% 逐字符一致；③P3-1 扩充 413 守门用例至 1 MiB、10 MiB、raw net socket 持续流式传输以及 64 KiB 精确边界校验（单测扩充至 15 项），`smoke:feedback` 超限档位提升至 1 MiB；四道门禁全绿（35 套 / 329 项单测全绿，Next.js 生产构建 18/18 页面通过） |
| **2026-09-18** | `5a55ac64` 独立审查 Round 2 复查（用户反馈系统 Feedback Round 1 缺陷闭环） | [`2026-09-18-feedback-workbuddy-code-review-round2-handoff.md`](2026-09-18-feedback-workbuddy-code-review-round2-handoff.md) | 复查全量 `861ad25a..5a55ac64`（22 文件，+2218/−239）与修复增量 `8657f299..5a55ac64`（9 文件，+879/−111）；**判定未通过**：0×P0/P1，**2×P2、1×P3**。Round 1 五项中 **P1-1/P3-3/P3-4/P3-5 经独立探针证实真正闭环**（真实生产服务端同 IP 前 5 次 `201` + 第 6 次 `429 retry-after=600` + 仅新增 5 文件；65536→400 / 65537→413 字节语义；伪造 locale 与 `zh-CN` → `"unknown"`、`ar` 保留；smoke 两次连跑全绿），**P2-1 仅部分闭环**：`feedback-api.ts:131-139` 在响应 `finish` 时 `request.destroy()` 掐断仍在上行的 socket → 128 KiB~10 MiB 在标准 Node HTTP 服务端 **3/3 轮 `ECONNRESET`（零字节响应）**、真实 `net` socket 持续上行 5 MiB **5/5 收到 0 字节**，Round 1 §四「65537 与 10 MiB 均返回 413（而非连接重置）」未达成（须改 lingering close / 读至 `end` 再回写）。**P2-2 本轮新引入回归**：`feedback-api.ts:43-57` 丢掉 `request.setEncoding("utf8")` 改逐 chunk `Buffer.toString()`，多字节 UTF-8 跨 chunk 被解为 `U+FFFD`，实测 CJK 正文切裂写入落盘长度 102（发送 100）、3 个替换字符、HTTP 仍 `201`（静默数据损坏）。**P3-1**：413 用例仅覆盖 `MAX+100`（70 KiB），MiB 级与持续上行竞态零覆盖，是 P2-1 逃逸门禁的直接原因。通过项、慢速分片上传、客户端半途 RST 断开、精确字节边界、locale 归一化等 60+ 次请求均符合预期；四道门禁独立复跑全绿（tsc 0 / lint 0 错误 0 警告 / vitest 35 套 324 例 / build 18/18） |
| **2026-09-18** | 用户反馈系统 (Feedback) Round 1 审查缺陷修复（1×P1, 1×P2, 3×P3 全闭环，待审交付） | [`2026-09-18-feedback-round1-remediation-handoff.md`](2026-09-18-feedback-round1-remediation-handoff.md) | 针对 WorkBuddy Round 1 源码审查报告（`42daaed`）实施 100% 缺陷闭环：①P1-1 修复限流守门 `feedbackLimiter.consume(clientKey)` 对象真值判断，改为判定 `.allowed` 并携带 `retry-after` 响应头阻断后续落盘；②P2-1 修复大报文 `request.destroy()` 掐断 socket 缺陷，升级为抛出 `PayloadTooLargeError` 并返回规范 HTTP 413 (`Payload too large`) + `Connection: close`，流排空后释放连接彻底消除连接重置；③P3-3 修复 UTF-16 字符计数漏洞，按 UTF-8 真实字节数累加，严格截断超过 64 KiB 的 CJK/Emoji 报文；④P3-4 接入 `isLocale()` 白名单校验，将非法、超长伪造值归一化为 `"unknown"`，合法语种保持原样；⑤P3-5 新增 `src/server/feedback-api.test.ts` 真实 HTTP 路由测试（10 项测试全绿）并在 `tools/smoke-feedback.ts` 补齐 413 与 429 断言；四道门禁全绿（35 套 / 324 项单测全绿，Next.js 生产构建 18/18 页面通过） |
| **2026-09-18** | `8657f299` 独立审查 Round 1 复查（用户反馈系统 Feedback 扁平化存储与纯文本 JSON 协议） | [`2026-09-18-feedback-workbuddy-code-review-round1-handoff.md`](2026-09-18-feedback-workbuddy-code-review-round1-handoff.md) | 复查全量 `861ad25a..8657f299`（18 文件，+1451/−240）；**判定未通过**：0×P0，**1×P1、1×P2、3×P3**。**P1-1** `src/server/online-server.ts:251` 限流守门 `if (!feedbackLimiter.consume(...))` 恒为 false（`consume()` 返回 `RateLimitResult` 对象非布尔）→ **429 分支为死代码**，真实生产服务端实测同 IP 连续 8 次 `POST /api/feedback` 全部 `201`、`429` 计数 0、8 个文件照常落盘，10 分钟 5 次 IP 限流完全失效（`tsc`/`lint` 因对象真值化合法而无法拦截）。**P2-1** 请求体超 64 KiB 走 `request.destroy()` 摧毁 socket，实测 65537 字节 → `UND_ERR_SOCKET`、70 KiB → `UND_ERR_SOCKET`、10 MiB → `ECONNRESET`，方案 `:107` 要求的 `413` 永不返回，`.catch` 的 500 落点不可达。**P3-3** 体积守门按 UTF-16 code unit 计数（66014 字节 CJK 载荷通过 64 KiB 检查，最坏放行约 3 倍）。**P3-4** `locale` 未按 `:113` 白名单校准（3011 字符含 `<script>` 伪造值原样落盘）。**P3-5** `/api/feedback` 路由层零测试、`smoke:feedback` 缺 429 与超限断言，是 P1 逃逸四道门禁的直接原因。通过项：存储契约实测有效（40 并发 → 40 个命名合规文件、0 `.tmp-` 残留、0 不可解析 JSON）、6 语种 SSG（`build` 18/18，`/[locale]/feedback` = `● (SSG)` ×6）+ `ar` RTL + `/xx/feedback` 404 + `/feedback` 307 均实测正确；四道门禁独立复跑全绿（tsc 0 / lint 0 错误 0 警告 / vitest 34 套 314 例 / build 18/18） |
| **2026-09-18** | 用户反馈系统 (Feedback) 源码实现与待审交付 | [`2026-09-18-feedback-and-log-collection-impl-handoff.md`](2026-09-18-feedback-and-log-collection-impl-handoff.md) | 依据用户最新决策与审查反馈落地极简扁平存储用户反馈系统：①服务端实现 `FeedbackStore` 原子落盘（`.tmp-*` 更名），采用紧凑 UTC 14 位时间戳命名 `${YYYYMMDD-HHmmss}-${feedbackId}.json`，单一 `data/feedback/` 扁平存储零子目录；②首版纯文本 JSON 协议（64 KiB 上限），彻底去除图片原生库依赖与 EXIF/GPS 泄露风险；③免注册直接提交，可选填写邮箱；④支持 6 种官方语言多语言字典、SSG 预渲染、RTL 排版镜像与暗色主题；⑤四道门禁全绿（34 套 / 314 项单测全绿，Next.js 18/18 页面构建通过，smoke:feedback 自动化烟测通过） |
| **2026-09-18** | 访客身份持久化与自愈 Round 3 审查缺陷修复（1×P3 全闭环，待审交付） | [`2026-09-18-persistent-guest-identity-round3-remediation-handoff.md`](2026-09-18-persistent-guest-identity-round3-remediation-handoff.md) | 针对 WorkBuddy Round 3 源码复查报告（`48b6c1a`）实施 100% 缺陷闭环：新建 `src/components/hooks/useRoomChat.test.ts` 全链路驱动测试套件，补齐四大守门用例（M-2 防分身死 token 复活、M-1 超时看门狗防锁死、P3-4 分身重发写回、P3-4 主页首发写回），经 4 项变异探针逐一实测变红验证（`expected 'dead-primary-token-12345' to be undefined`、`expected true to be false`、`expected null to be 'freshly-minted-avatar-token'`、`expected null to be 'initial-minted-primary-token'`）；降级重命名 `chat-send-gate.test.ts` 歧义名称；四道门禁全绿（33 套 / 306 例单测全绿，Next.js 生产构建通过） |
| **2026-09-18** | `663a001` 独立审查 Round 3 复查（访客身份持久化、30天滑动TTL与全链路静默自愈 · 源码实现复查） | [`2026-09-18-workbuddy-code-review-round3-handoff.md`](2026-09-18-workbuddy-code-review-round3-handoff.md) | 复查全量 `cb01d11..663a001`（21 文件，+1823/−54）与修复增量 `2c1fba6..663a001`（11 文件，+564/−11）；**判定未通过**：0×P0/P1/P2，**1×P3**。Round 2 四项 P3 的**代码修复经独立探针证实均有效**（P3-1 压缩守门改用 `rewriteJsonlFile` 真实调用计数 + 死行超阈值边界用例，变异后两用例均变红且收到 5 次 rewrite；P3-2 重发 payload 已改显式构造、不再经 `getActivePlayer()` 复活死 token；P3-3 重发已重新 `gate.begin()` 挂看门狗；P3-4 `PublicChatAck` 回传 token + 客户端落盘，实测自愈换发后新连接复用同一 token 且 `guest-sessions.jsonl` 恒 1 行、`public-chat:messages` 广播不含 token）。缺陷 P3-1：**本轮三项客户端自愈修复（P3-2/P3-3/P3-4）在 `useRoomChat` 全链路零守门**——`src/components/hooks/` 下无 `useRoomChat.test.ts`，新增的 `chat-send-gate.test.ts:74-101` 仅直接重测闸门类（从未 import 该 hook，hook 是否调用 `gate.begin()` 结果完全相同），`room-state-utils.test.ts:203-239` 只在用例内手工重建 payload 并断言本地对象字面量属性为 undefined（同义反复）；变异实测：①删除重发看门狗 + 删除重发/首发 token 写回 → **32 套 / 302 例全绿**，②将重发 payload 回退为 `getActivePlayer()`（即 Round 2 P3-2 缺陷原文）→ **32 套 / 302 例全绿**，Round 2 §四 明文要求的"变异探针必须变红"未达成。独立验证：1 项端到端运行时探针（真实 Socket.IO + 真实落盘 store：死 token 失败 ack 无 token 字段、`room:error` 抑制计数 0、跨连接 token 复用、广播字段白名单、`resetGuestIdentity`+有效 token 边界）+ 3 项变异探针；`tsc`/`lint` 0 问题、`vitest` 32 套 302 例连续 3 次全绿、`build` 11/11 页通过 |
| **2026-09-18** | 访客身份持久化与自愈 Round 2 审查缺陷修复（4×P3 全闭环，待审交付） | [`2026-09-18-persistent-guest-identity-round2-remediation-handoff.md`](2026-09-18-persistent-guest-identity-round2-remediation-handoff.md) | 针对 WorkBuddy Round 2 源码审查报告（`d39a2dd`）实施 100% 缺陷闭环：①P3-1 守门单测改用 `rewriteJsonlFile` spy 观测真实重写次数（断言 5 次创建 0 次 rewrite），增补 200 行超阈值边界压缩用例（断言恰 1 次 rewrite 且收拢至 20 行），经变异探针实测两用例均变红；②P3-2 `useRoomChat` 移除 `getActivePlayer()` 读存储回落，显式构造全新 payload，防范分身标签页复活 localStorage 死 token 导致二次失败与红字；③P3-3 公聊自愈重发接入 `ChatSendGate` 看门狗与超时复位，根治未响应 ack 下发送按钮永久锁死；④P3-4 `PublicChatSnapshot` 扩展 `guestToken`，服务端透传至 ACK 且客户端落盘复用，实现身份长效保持与文件行数收敛；四道门禁全绿（32 套 / 302 例单测全绿，Next.js 生产构建通过） |
| **2026-09-18** | `2c1fba6` 独立审查 Round 2 复查（访客身份持久化、30天滑动TTL与全链路静默自愈 · 源码实现修复复查） | [`2026-09-18-workbuddy-code-review-round2-impl-handoff.md`](2026-09-18-workbuddy-code-review-round2-impl-handoff.md) | 复查全量 `cb01d11..2c1fba6`（17 文件，+1265/−49）与修复增量 `cbb44e3..2c1fba6`；**判定未通过**：0×P0/P1/P2，**4×P3**。Round 1 五项缺陷的**代码修复经独立探针证实有效**（P2-1 存活 15 行 / 阈值 10 下 5 次落盘 = 5 次纯追加、0 次 rewrite，死行 185 行时恰 1 次 rewrite 且行数 200→20；P3-4 三次模拟页面加载 `guest-sessions.jsonl` 恒 1 行、token 一致；P3-2/P3-3 经代码核验闭环）。缺陷 P3-1：新增 P2-1 守门用例为**恒真断言**——断言"5 次 createSession 后行数 = 初值 + 5"在压缩与不压缩两实现下**恒成立**，变异探针（`reset` 回退为 `appended = liveLines`）下该用例与 `accounts.test.ts` 全量 16 例均**仍绿**（改以 `rewriteJsonlFile` 计数则收到 5 次 rewrite 变红），Round 1 复审验收标准"变异探针必须变红"未达成，写放大回归零守门。P3-2：`useRoomChat.ts:144-149` 自愈重发用 `getActivePlayer()` 重建 payload，ephemeral 标签页下 `clearGuestToken()` 故意不清 localStorage、`readGuestToken()` 又回落读出同一个失效主 token → 重发必然二次失败并上屏 `guest-session-invalid` 红字（探针两段实测：无 token+resetGuestIdentity 成功 / 带回落 token+resetGuestIdentity 失败），Round 1 P3-1 未真正闭环。P3-3：重发 emit 绕过 `ChatSendGate`（无看门狗），ack 被静默丢弃时 `isSendingPublicChat` 永久为 true、发送按钮锁死。P3-4：`PublicChatAck` 不回传 `guestToken` 且客户端不写回，自愈成功后新身份无处落盘，下次加载身份重置并重新铸会话（行数 +1）。独立验证：4 项运行时探针 + 1 项变异探针 + 1 项压缩边界探针；`tsc`/`lint` 0 问题、`vitest` 32 套 297 例全绿（`build` 未复跑） |
| **2026-09-18** | 访客身份持久化与自愈 Round 1 审查缺陷修复（1×P2 + 4×P3 全闭环） | [`2026-09-18-persistent-guest-identity-round1-remediation-handoff.md`](2026-09-18-persistent-guest-identity-round1-remediation-handoff.md) | 针对 WorkBuddy Round 1 源码审查报告（`92b46aa`）实施 100% 缺陷闭环：①P2-1 升级 `JsonlCompactionTracker.reset(liveLines, totalLines)`，`liveLines >= threshold` 时进入增量预算模式，彻底杜绝文件行数达阈值后每次写盘的全量重写，守门测试断言加载超阈值文件后 5 次创建均为纯追加；②P3-1 公共聊天（`public-chat:send`）服务端抑制 `guest-session-invalid` 的 `room:error` 广播，客户端 `useRoomChat` 捕获失效并带 `resetGuestIdentity` 自动重试；③P3-2 重连自愈（`reconnectHandlerRef`）改为调用 `clearClosedRoom`，彻底清除内存 room 态与 URL 避免幽灵房间；④P3-3 重构坏行容错单测，写入真实哈希行并断言反向解析成功，通过变异探针；⑤P3-4 `PresenceSnapshot` 扩展 `guestToken`，`presence:join` 回传并由客户端持久化复用，阻断匿名会话文件行数无限递增；32 套 / 297 例单测全绿，四道门禁全绿 |
| **2026-09-18** | `cbb44e3` 独立审查 Round 1（访客身份持久化、30天滑动TTL与全链路静默自愈 · 源码实现审查） | [`2026-09-18-workbuddy-code-review-round1-impl-handoff.md`](2026-09-18-workbuddy-code-review-round1-impl-handoff.md) | 审查全量 `cb01d11..cbb44e3`（12 文件，+853/−42）；**判定未通过**：0×P0/P1，**1×P2 + 4×P3**。P2-1 压缩记账口径错误——`loadFromFile` 的 `reset(lineCount)` 与 `noteAppend()` 语义混用，文件行数 ≥ 阈值 2,000 后**每次落盘都全量重写整份日志**（spy/inode 实测 2,100 行时 3 次 createSession = 3 次 rewrite；50,000 行时单次 63.3ms 同步阻塞事件循环，旧 10k 对照 0.57ms）。P3-1 `acknowledgeAndBroadcastPublicChat`（`room-socket.ts:1082`）未纳入抑制白名单且 `useRoomChat` 无自愈，8 入口探针矩阵中唯 `public-chat:send` 泄漏 `room:error`。P3-2 重连自愈分支（`useRoomSocket.ts:270-275`）未清内存房间态，用户滞留"幽灵房间"并触发 `not-room-member` 红字。P3-3 坏行容错用例断言恒真（`accounts.test.ts:422`），验收标准 3 无守门。P3-4 无 token 的匿名事件每次新连接即铸造并落盘一条不可复用会话（3 次页面加载 = 3 行）。自建 11 项独立探针（抑制矩阵 / 跨重启滑动 TTL / 压缩重写计数 / 50k 容量成本 / 坏行容忍 / 匿名会话铸造计数） |
| **2026-09-18** | 访客身份持久化、30天滑动TTL与全链路静默自愈源码实现与待审交付 | [`2026-09-18-persistent-guest-identity-impl-handoff.md`](2026-09-18-persistent-guest-identity-impl-handoff.md) | 完成方案收敛后的全栈源码实现与测试覆盖：①服务端升级 30 天滑动 TTL、50,000 会话容量、基于 JSONL 的落盘持久化与重启加载（含损坏行容错）、60s 节流落盘并严格挂接 compaction tracker、基于 lastSeenAt 的 LRU 淘汰、扩展 resetGuestIdentity 协议并抑制 room:error 广播；②客户端引入标签页显式 EphemeralSession 状态机与 shouldBeEphemeral 双重守卫，物理隔绝分身标签页覆写 localStorage 主身份，移除死 token 回退，并在建房/加房/重连/大厅/匹配全链路落地静默重签与无感自愈；③32 个单元测试套件 294 项用例全部通过（含分身隔离集成级守门用例），本地四道门禁 100% 全绿；准备派发 WorkBuddy 独立代码审查（Round 1） |
| **2026-09-18** | 访客身份持久化技术方案 Round 2 审查缺陷闭环与第 3 轮收敛定稿（转入代码实现） | [`2026-09-18-persistent-guest-identity-plan-final-convergence-handoff.md`](2026-09-18-persistent-guest-identity-plan-final-convergence-handoff.md) | 针对 WorkBuddy Round 2 审查指出的缺陷实施 100% 闭环：①P2-1 将 Ephemeral 提升为标签页级 Session 显式状态（`isEphemeralSession()`、`markEphemeralSession()`、`clearEphemeralSession()` 与内部双重守卫），彻底封死 `applyRoomAck` 与 `getActivePlayer` 的主身份覆写泄漏，守门测试断言 `localStorage` 逐字节不变；②P3-1 客户端 `room-state-utils.ts` 同步补齐 `PlayerAuthPayload.resetGuestIdentity?: boolean`，类型契约严格对齐；③吸收建议 1（补齐 `joinRoomByTarget` 分身代码）与建议 2（`lastSeenAt` 60s 节流落盘计入 `this.compaction.noteAppend()`）；依据 Rule 7/Rule 11 & §4.3 分支 C，宣布设计方案达成收敛定稿，正式转入代码实现阶段 |
| **2026-09-18** | `a6b7975` 独立审查 Round 2 复查（访客身份持久化技术设计方案 · 纯文档方案审查） | [`2026-09-18-workbuddy-code-review-round2-handoff.md`](2026-09-18-workbuddy-code-review-round2-handoff.md) | 复查全量 `d276991..a6b7975`（6 文件，+669/−1，全为文档）；Round 1 六项缺陷中 5 项经代码路径核验闭环（P2-2 `resetGuestIdentity` 语义自洽、P2-3 抑制点覆盖两条入口、P3-1 匹配纳入自愈、P3-2 容量与 LRU 口径一致、P3-3 第三优先级移除后死 token 无复活入口）；自建 2 项探针（分身隔离 Storage 语义全链路模拟 + `tsc` 类型契约复现）**证伪修复 2 项**：①P2-1 未真正闭环——ephemeral 仅为单次调用参数，`applyRoomAck`（不传 options）与 `getActivePlayer`→`getOrCreatePlayerId`（不传 options）两条主身份写路径仍覆写 `localStorage`，实测主 token/playerId 双双重置为新标签页继承分身身份；②P3-1 客户端 `PlayerAuthPayload` 未同步扩展，实测 `TS2353` 必卡门禁 1；**判定未通过，需修复闭环**（第 2 轮，上限 3 轮内） |
| **2026-09-18** | 访客身份持久化技术方案 Round 1 审查缺陷修复与设计定稿（3×P2 + 3×P3 全闭环） | [`2026-09-18-persistent-guest-identity-plan-round1-remediation-handoff.md`](2026-09-18-persistent-guest-identity-plan-round1-remediation-handoff.md) | 针对 WorkBuddy Round 1 审查指出的 6 项缺陷完成 100% 闭环修订：①P2-1 在 `persistGuestToken` 引入 `ephemeralOnly` 严格单写 sessionStorage，duplicate-player 重试走独立分身，严禁覆盖 localStorage 主身份；②P2-2 扩展 `resetGuestIdentity` 协议并在 token 失效时服务端主动剥离 socket.data 缓存，消除死循环；③P2-3 抑制失败 ack 的全局 room:error 广播并由客户端自愈置空 setError，断绝红字外溢；④P3-1 在线匹配纳入自愈状态机；⑤P3-2 扩容至 50,000 会话与滑动活跃 LRU 淘汰；⑥P3-3 移除 readRoomSession 死 token 回退与全量同步清理 |
| **2026-09-18** | `d7ceff2` 独立审查 Round 1（访客身份持久化技术设计方案 · 纯文档方案审查） | [`2026-09-18-workbuddy-code-review-round1-handoff.md`](2026-09-18-workbuddy-code-review-round1-handoff.md) | 审查全量 `d276991..d7ceff2`（4 文件，+355/−1，全为文档）；四大支柱与四条根因经代码比对属实、落盘路径与 `.gitignore`/systemd 约定自洽；自建探针 3 场景（真实 Socket.IO 双连接 ×2 + 方案 §3.3 逐字实现 Storage 契约模拟 ×1）**证伪核心机制 2 项**：`ephemeralOnly` 清理无效且分身双写覆写主身份（P2-1）、服务端 `socket.data.guestToken` 回退使自愈二次失败（P2-2）、失败 ack 无条件 `room:error` 使"永不报错"不可达（P2-3）；另有 3×P3（匹配入口缺覆盖 / 30 天 TTL 与 maxEntries 淘汰冲突 / 死 token 残留房间会话）；**判定未通过，需修复闭环** |
| **2026-09-18** | 访客身份持久化、30天有效期与全链路自愈技术设计方案交付 | [`2026-09-18-persistent-guest-identity-plan-handoff.md`](2026-09-18-persistent-guest-identity-plan-handoff.md) | 针对好友房与大厅中频发的“Guest session is invalid”报错及跨会话长效保留身份诉求，制定完整技术方案：①客户端 Storage 双写与优先级机制（localStorage 长效主身份 + sessionStorage 标签页临时覆盖与分身保护）；②服务端 GuestSessionStore 接入 JSONL 文件落盘持久化（data/accounts/guest-sessions.jsonl，服务重启不丢凭证）；③TTL 扩充为 30 天滑动过期时钟；④全链路静默自愈状态机（建房/大厅/重连遇失效自动重新签发并无感重试，彻底杜绝 UI 报错） |
| **2026-09-16** | `0197cee` 独立审查 Round 5 复查（对局持久化/语言切换/外观保持 实现交付终审） | [`2026-09-16-workbuddy-code-review-persistence-impl-round5-handoff.md`](2026-09-16-workbuddy-code-review-persistence-impl-round5-handoff.md) | 复查全量 `9763c09..0197cee`（29 文件，+2352/−124）；Round 4 P3-1 闭环核验完全成立（`pendingAiResolveRef` 主动决议 null 消除 await 死锁、`GameShell.tsx` layout effect cleanup 取消思考并复位守卫、deps 精简稳定、`smoke:persistence` S-F 5/5 全通）；自建负向竞态探针实测（AI 搜索中切语言重挂载连续 6s 采样 stones 恒为 2、无双落子、按钮解锁、无 draw 误判）；四道门禁全绿；**判定无 P0~P3 缺陷，审查通过（PASS）** |
| **2026-09-16** | 对局持久化与语言平滑切换 Round 4 审查缺陷修复（1×P3 闭环与 S-F 冒烟守门） | [`2026-09-16-game-persistence-round4-remediation-handoff.md`](2026-09-16-game-persistence-round4-remediation-handoff.md) | 闭环 WorkBuddy Round 4 审查指出的 P3-1 缺陷：`useAiGame` 引入 `pendingAiResolveRef`并在取消与卸载时主动决议未决 Promise、彻底复位 `isAiThinking`；`GameShell.tsx` 引入 `aiGameRef` 稳定依赖项，在 layout effect 清理中取消 AI 思考并复位恢复守卫，消除 StrictMode 双调用导致的 AI 回合永久死锁；`smoke:persistence` 补齐 Scenario S-F（切语言软导航 + AI 回合恢复）与自启动服务、可靠进程清理；四道门禁全绿（31 套 / 282 项单测全通，Next.js 生产构建通过，smoke:persistence 5 大场景全绿） |
| **2026-09-16** | `d68a8de` 独立审查 Round 4 复查（对局持久化/语言切换/外观保持 实现交付） | [`2026-09-16-workbuddy-code-review-persistence-impl-round4-handoff.md`](2026-09-16-workbuddy-code-review-persistence-impl-round4-handoff.md) | 复查全量 `9763c09..d68a8de`（27 文件）；Round 3 P1-1（`useIsHydrated` 水合门控）与 P3-1（变异守门套件）经代码路径核验闭环，需求 1~5 映射无遗漏；真机 CDP **dev/生产差分探针**发现 **1×P3**（dev StrictMode 下切语言软导航重挂载恢复「AI 回合」对局永久死锁：清理路径只 terminate worker 不 settle 搜索 Promise、`hasRestoredBootRef` 单次守卫阻断重试，实测 dev 12s 棋子恒 1 + 按钮恒锁，生产 1s 内 AI 落子正常；smoke:persistence 未覆盖该组合故未拦截）；**未通过、待修复闭环** |
| **2026-09-16** | 对局持久化与语言平滑切换 Round 3 审查缺陷修复（1×P1 + 1×P3 闭环与 CDP 冒烟守门） | [`2026-09-16-game-persistence-round3-remediation-handoff.md`](2026-09-16-game-persistence-round3-remediation-handoff.md) | 闭环 WorkBuddy Round 3 审查指出的全部缺陷：P1-1 引入 `useIsHydrated` 拦截水合首轮提前消费守卫，并在 `initialSnapshot` 与 layout effect 中统一使用 `canRestoreBootGame` 纯函数，彻底修复 F5 刷新恢复 0 子与自愈失效；P3-1 移除同义反复单测，增设 `canRestoreBootGame` 独立变异守门套件；新增 `tools/smoke-persistence.ts` 固化浏览器级 CDP 冒烟自动化测试（覆盖 S-A/S-E/S-B/S-C 全场景）；四道门禁全绿（31 套 / 282 项单测通过，生产构建全通） |
| **2026-09-16** | `0fc7bb6` 独立审查 Round 3 复查（对局持久化/语言切换/外观保持 实现交付） | [`2026-09-16-workbuddy-code-review-persistence-impl-round3-handoff.md`](2026-09-16-workbuddy-code-review-persistence-impl-round3-handoff.md) | 复查增量 `c37c328..0fc7bb6`（源码 3 文件）；真机 CDP 4 组探针：Round 2 P2-1 目标场景（room 隔离）复验通过，但 **1×P1**（修复引入回归：React 19.2 水合渲染取 server snapshot，恢复 layout effect 首轮即落入失配分支无条件置位 `hasRestoredBootRef`，重渲染后被守卫早退——F5 恢复与思考中刷新自愈全部静默失效，R1-P1-1 完整回归；实测 local 3 手局刷新 stones=0、软导航对照组 stones=3）+ **1×P3**（新增测试断言同义反复，对产品守卫零验证力）；**未通过、待修复闭环** |
| **2026-09-16** | 对局持久化与语言平滑切换 Round 2 审查缺陷修复（1×P2 闭环） | [`2026-09-16-game-persistence-round2-remediation-handoff.md`](2026-09-16-game-persistence-round2-remediation-handoff.md) | 闭环 WorkBuddy Round 2 代码审查指出的 P2-1/R2 缺陷：在 `GameShell.tsx` 恢复 layout effect 中补齐 `bootActiveGame.mode === bootMode` 严格模式匹配守卫，失配分支挂载即时决议自愈标记释放模式切换按钮；在 `useAiGame.ts:commitAiTurn` 核心层增加 `if (mode !== "ai") return;` 深度防御守卫，断绝幽灵搜索泄漏；增补模式失配防污染单元测试；四道门禁全绿（31 套 / 278 项单测通过，生产构建全通） |
| **2026-09-16** | `8ed958f` 独立审查 Round 2 复查（对局持久化/语言切换/外观保持 实现交付） | [`2026-09-16-workbuddy-code-review-persistence-impl-round2-handoff.md`](2026-09-16-workbuddy-code-review-persistence-impl-round2-handoff.md) | 复查增量 `1ff7501..8ed958f`（源码 4 文件）；真机 CDP 9 项独立验证确认 Round 1 双缺陷 100% 闭环（F5 恢复全语义 + 恰一次搜索契约 + AI 参数零漂移）；0×P0/P1/P3，**1×P2**（修复引入：`GameShell.tsx:127-171` 恢复 layout effect 缺少模式匹配守卫，`?room=` 整页加载幽灵恢复存量 AI 对局并触发幽灵搜索 post=1/term=0、模式按钮锁定、违背在线/本地生命周期隔离；local 变体同样污染状态）；**未通过、待修复闭环** |
| **2026-09-16** | 对局持久化与语言平滑切换 Round 1 审查缺陷修复（1×P1 + 1×P2 闭环） | [`2026-09-16-game-persistence-round1-remediation-handoff.md`](2026-09-16-game-persistence-round1-remediation-handoff.md) | 闭环 WorkBuddy Round 1 代码审查指出的全部缺陷：P1-1 引入 `restoreBootActiveGameSnapshot` 纯函数并由 `useIsomorphicLayoutEffect` 在水合后一次性采纳，同步 commit 棋盘/棋谱/胜负线与 AI 难度/先手/`openingSeed`，彻底解决 F5 刷新后 0 子及后续落子覆盖存储；P2-1 在 Boot 快照采纳时即时裁决自愈握手，补齐 `hasHealedRef` 初始置位，并对兜底 effect 增加 `moves.length === baseline` 步数比对守卫，彻底杜绝人类首手误触发重复搜索；四道门禁全绿（31 套 / 278 项单测通过，生产构建全通） |
| **2026-09-16** | `1ff7501` 独立审查 Round 1（对局持久化/语言切换/外观保持 实现交付） | [`2026-09-16-workbuddy-code-review-persistence-impl-round1-handoff.md`](2026-09-16-workbuddy-code-review-persistence-impl-round1-handoff.md) | 逐文件核验 19 文件 diff（+1035/−120）；真机 CDP（生产构建 + `online-server.ts`）12 项独立验证：路由参数保持、主题 161 帧零翻白、四级启动优先级、在线对局中切语言 Socket 存活且服务端推送可达等均通过；0×P0/P3，**1×P1 + 1×P2**（P1-1 硬刷新后 `useBootSnapshot` 水合首轮 server snapshot 被 `useState` 惰性初始化器落定，local/ai 活跃对局恢复静默失效且后续落子覆盖存储——软导航恢复正常构成对称性根因证据；P2-1 boot 进 AI 后首手自愈握手误触发重复搜索 post2/term1/ctor2，对照 D3 为 post1/term0/ctor0）；**未通过、待修复闭环** |
| **2026-09-16** | 对局持久化、语言平滑切换与外观保持功能与代码交付 | [`2026-09-16-game-persistence-and-locale-switching-delivery-handoff.md`](2026-09-16-game-persistence-and-locale-switching-delivery-handoff.md) | 全面落地纯函数 `computeLocaleSwitchHref` 路由保持与 3 阶段 SSG 安全执行流；权威主题源结合 `useIsomorphicLayoutEffect` 预绘制回写与 Scoped CSS 兜底杜绝暗色切语言闪白；`sessionStorage` 对局持久化与防御性运行时校验；四级启动判定优先级与实例级快照，联机大厅 0 手与人机 0 手不跌落 local；AI 首帧注入与思考自愈握手；四道门禁全绿（31 套 / 274 项单测通过，生产构建全通） |
| **2026-09-16** | Round 3 审查缺陷收敛与方案最终定稿（Rule 11 / Branch C） | [`2026-09-16-game-persistence-plan-final-convergence-handoff.md`](2026-09-16-game-persistence-plan-final-convergence-handoff.md) | 闭环 WorkBuddy Round 3 终审指出的全部 2 项 P3 缺陷：P3-1 将 §4.1 兜底 CSS 声明集 100% 对齐本仓 `globals.css:35-67` 权威暗色变量，彻底消除伪代码外部变量与暗底深字对比度问题；P3-2 扩充工作区键写入点至 URL 启动与退房路径，确保链接进房退房后刷新稳健停留在联机大厅；依据 Rule 11 / Branch C 完成 3 轮上限收敛并定稿，正式转入代码实现阶段 |
| **2026-09-16** | `8148e81` 独立审查 Round 3 复查（对局持久化/语言平滑切换/外观保持 技术设计方案） | [`2026-09-16-workbuddy-code-review-round3-handoff.md`](2026-09-16-workbuddy-code-review-round3-handoff.md) | 逐文件核验 10 文件 diff（+792/−8，全部 `.md`，**零源码改动**），复查增量 `c7a5ae3..8148e81`；确认 Round 2 六项缺陷在文本契约层面逐项闭环；真机 CDP 两项独立验证（生产构建）：① 注入式 A/B 证伪 §4.1 兜底 CSS 块——8/12 变量在全仓不存在、漏掉 `globals.css:35-67` 的 27 项声明（含 `color-scheme`/`--ink`/`--board*`），实测只压暗背景而文字仍为 `#18212f`（对比度 ≈1.14:1 不可读）、棋盘仍浅色，换用权威声明集后逐项正确；② 机制取证证实 Round 2 P3-2 方向成立——剥离由 React 提交期 mutation 经 `removeAttributeNode` 触发、与页面树替换同一 MutationObserver 批次（同刻 `t=1329.6`），提交后回写 180 帧零缺失帧，不回写对照第 1 帧即翻白；0×P0/P1/P2，**2×P3**（P3-1 兜底声明集非本仓库主题，字面落地即"暗底 + 浅色主题文字"破版并向 `globals.css` 注入 8 项死声明与外来色板；P3-2 §3.2 工作区键写入点覆盖不到经 `?room=` 直接进房的入口而 §3.4 矩阵已断言 `leaveRoom` 后为 `room`，该路径退房后刷新/切语言跌落 `local`，切语言子路径相对现状构成回归）；**未通过、待修复闭环**（方案架构已收敛，两项 P3 可并入实现期吸收，详见报告 §四） |
| **2026-09-16** | Round 2 审查缺陷修复（1×P2 + 5×P3 全闭环）与设计定稿 | [`2026-09-16-game-persistence-plan-round2-remediation-handoff.md`](2026-09-16-game-persistence-plan-round2-remediation-handoff.md) | 闭环 WorkBuddy Round 2 审查指出的全部 6 项缺陷：P2-1 明确权威主题解析源为存储值 ?? 系统偏好，彻底解决仅系统暗色无存储用户切语言丢主题；P3-1 限定 CSS 兜底选择器为 `:root:not([data-theme])` 消除手选浅色反向闪烁；P3-2 确立预绘制前回写契约（useLayoutEffect），从调度层保证零无属性已绘制帧；P3-3 敲定确定的三阶段路由读取流，消除 SSG Bailout 与水合不一致；P3-4 正交解耦模式切换与单模式重开，消除不可达分支；P3-5 引入工作区记录持久化与四级启动优先级，联机大厅 0 手与人机 0 手切语言/刷新不跌落 local；四道门禁全绿 |
| **2026-09-16** | `c7a5ae3` 独立审查 Round 2 复查（对局持久化/语言平滑切换/外观保持 技术设计方案） | [`2026-09-16-workbuddy-code-review-game-persistence-round2-handoff.md`](2026-09-16-workbuddy-code-review-game-persistence-round2-handoff.md) | 逐文件核验 8 文件 diff（+495/−8，全部 `.md`，**零源码改动**），重点复查 `90237ce..c7a5ae3` 增量；真机 CDP 两组探针（生产构建）实测：空 `localStorage` + 系统偏好暗色下切语言 `data-theme` **仍被剥离**（同节点原地删除、下一帧即 `bg=rgb(246,247,242)`、157 帧中 154 帧无属性），导航后写回 `dataset.theme` 可存活（机制可行），CSS 级 A/B 证伪无作用域媒体兜底（显式浅色被劫持为暗色）；0×P0/P1，**1×P2 + 5×P3**（P2-1 回写数据源只写 `localStorage` 漏掉系统偏好路径，Round 1 P2-1 未闭环且正落在其承诺复审的复现路径；P3-1 兜底未限定 `:not([data-theme])` 致反向闪烁；P3-2 未规定绘制前回写致"零浅色帧"不可保证；P3-3 `search` 读取时机仍为"或"，水合不一致与中键丢 `?room=` 两解并存；P3-4 §3.4 矩阵对同一调用给出两种清理对象且含不可达分支；P3-5 §3.2 无法重建"用户已选工作区"，大厅/0 手人机跌落 `local`）；**未通过、待修复闭环** |
| **2026-09-16** | Round 1 审查缺陷修复（2×P2 + 3×P3 全闭环）与设计定稿 | [`2026-09-16-game-persistence-plan-round1-remediation-handoff.md`](2026-09-16-game-persistence-plan-round1-remediation-handoff.md) | 闭环 WorkBuddy Round 1 审查指出的全部 5 项缺陷：P2-1 确立切语言软导航主题属性回写机制与 CSS 兜底；P2-2 将启动快照升级为实例级（useRef）挂载求值；P3-1 规避 SSG 预渲染裸调 useSearchParams；P3-2 建立控制流存储清理矩阵杜绝脏局复活；P3-3 规范 useAiGame 首帧注入与自愈握手显式传参；四道门禁全绿 |
| **2026-09-16** | `52c17a6` 独立审查 Round 1（对局持久化/语言平滑切换/外观保持 技术设计方案） | [`2026-09-16-workbuddy-code-review-game-persistence-round1-handoff.md`](2026-09-16-workbuddy-code-review-game-persistence-round1-handoff.md) | 逐文件核验 4 文件 diff（+257/−1，纯文档）；真机 CDP 三组独立验证（软导航非整页刷新但组件确实重挂载、在线房间 `?room=` 丢失并跌落 `local`、**暗色下切语言 `data-theme` 被剥离致背景 `rgb(18,20,23)→rgb(246,247,242)`，dev+prod 双复现**）；0×P0/P1，**2×P2 + 3×P3**（P2-1 切语言主题丢失且方案 §4.1 断言与实测相反并遗漏实现项；P2-2 启动快照粒度未定义，沿用 `client-boot-state.ts` 模块级缓存会使切语言恢复链静默失效；P3-1 `useSearchParams` 读法会在 SSG 的 `/[locale]` 触发 CSR bailout 破坏门禁 4；P3-2 §3.4 清理触发点与 `getModeChangeDecision` 的 `direct` 分支不匹配致脏对局复活；P3-3 AI 自愈握手未定义恢复设置注入时序，AI 先手场景将按默认设置触发致落错色）；**未通过、待修复闭环** |
| **2026-09-16** | 对局持久化、语言平滑切换与外观保持方案设计 | [`2026-09-16-game-persistence-and-locale-plan-handoff.md`](2026-09-16-game-persistence-and-locale-plan-handoff.md) | 针对刷新丢局与语言切换跳回首页/丢房间号 Bug 制定完整技术设计方案；解耦纯函数 `computeLocaleSwitchHref` 保持路由与参数；设计 `StoredActiveGame` 与纯函数确定性回放；覆盖 AI 思考中被刷新的自愈握手机制；四道门禁全绿（29 套 / 248 项单测通过） |
| **2026-09-16** | Round 3 复查缺陷修复（P3-1）与分隔带断言精准化 | [`2026-09-16-unlisted-room-round3-findings-remediation-handoff.md`](2026-09-16-unlisted-room-round3-findings-remediation-handoff.md) | 重构 `LobbyMatchmaking.test.ts` 中针对单字连接词 `orJoinExisting` 的单测断言，采用正则精准捕获 `<span>` 内文本对比字典并处理 HTML 实体转义，彻底杜绝单字在类名和长标题匹配中的恒真问题；变异探针确认守门真实生效；四道门禁全绿（29 套 / 248 项单测 100% 通过，生产构建全通） |
| **2026-09-16** | `bca7674` 独立审查 Round 3 复查（不公开房间创建/加入交互解耦） | [`2026-09-16-workbuddy-code-review-unlisted-room-round3-handoff.md`](2026-09-16-workbuddy-code-review-unlisted-room-round3-handoff.md) | 逐文件核验 12 文件 diff（+729/−48）并单独复核本轮 `0dfce6b..bca7674` 5 文件；真实浏览器 CDP 覆盖 `en@1280×900` + `ar@390×844`（heading 序列 `H1→H2` 无跳级、`aria-describedby`/`aria-labelledby` 经 hydration 后仍解析、创建按钮 AX `description` = hint、`form` 可读名 `Join existing room`、分隔带已移出 AX 树、几何顺序与 RTL 零横向溢出、控制台零 error/warning）；独立复跑 `tsc`/`lint` 0 问题、`vitest --pool=vmForks` 29 套 / 248 例全绿、`npm run build` 成功（11 页），本地实跑 `smoke:lobby-ui` 确认本轮两处新断言通过于既有 `assertRoomError` 失败点之前；确认 Round 2 四项 P3 全部真实闭环；0×P0/1/2，**1×P3**（`LobbyMatchmaking.test.ts:89`/`:64-66` 对单字连接词 `orJoinExisting` 的整文档 `toContain` 断言在覆盖的 zh/ar 两语种下恒真——均为常驻 `createOrJoin` 的子串，变异探针删除分隔带 `<span>` 后仍通过，守门失效）；**未通过、待修复闭环** |
| **2026-09-16** | Round 2 复查缺陷修复（P3-1 至 P3-4）与标题大纲连续化 | [`2026-09-16-unlisted-room-round2-findings-remediation-handoff.md`](2026-09-16-unlisted-room-round2-findings-remediation-handoff.md) | 回填 `STATUS.md:11` 被审产品交付 `800a3cb`；加入卡片小标题升级为 `<h2>` 消除 h1→h3 标题层级跳跃；6 语种 `orJoinExisting` 收敛为纯连接词（or/或/ou/o/или/أو）且 divider 重加 `aria-hidden="true"`，消除语义重复与重复播报；`assertFriendsSectionStructure` 补充 `return null` 修复 20s 轮询等待契约；四道门禁全绿（29 套 / 248 项单测 100% 通过，生产构建全通） |
| **2026-09-16** | `800a3cb` 独立审查 Round 2 复查（不公开房间创建/加入交互解耦） | [`2026-09-16-workbuddy-code-review-unlisted-room-round2-handoff.md`](2026-09-16-workbuddy-code-review-unlisted-room-round2-handoff.md) | 逐文件核验 10 文件 diff（+558/−48）并单独复核本轮 `bb3be08..800a3cb` 5 文件；以真实浏览器 CDP 完成 AX 树取证（含 `aria-hidden` A/B 对照）、hydration 控制台零告警检查、`en/ru@1280`+`ar@390` 截图与几何实测；独立复跑 `vitest --pool=vmForks` 29 套 / 248 例全绿、`npm run build` 成功（11 页），并实跑 `smoke:lobby-ui`（dev/prod 两模式）确认本轮新增断言通过；证伪确认 Round 1 四项 P3 功能修复真实生效（组件回退 `165c56d` 后新增单测 2/3 失败）；0×P0/1/2，**4×P3**（`STATUS.md:11` 记录审查提交而非被审交付 `bb3be08`、新增 `<h3>` 造成 h1→h3 标题层级跳跃、分隔带与加入小标题 6/6 语种语义重复、`assertFriendsSectionStructure` 误用 `waitForValue` 致 20s 等待失效）；**未通过、待修复闭环** |
| **2026-09-16** | Round 1 审查缺陷修复（P3-1 至 P3-4）与大厅交互自动化守门 | [`2026-09-16-unlisted-room-round1-findings-remediation-handoff.md`](2026-09-16-unlisted-room-round1-findings-remediation-handoff.md) | 消除 `joinExistingRoom` 6 语种死键（挂载为加入卡片可见标题）；创建按钮接入 `aria-describedby`，分隔带移除 `aria-hidden` 保障无障碍可达；6 语种去除 "private/专属" 访问保护暗示词与免责对齐；新增 `LobbyMatchmaking.test.ts` 纯 Node 服务端渲染单测及 `tools/smoke-lobby-ui.ts` 结构与 390px RTL 展开态烟测断言，硬性阻断结构性回退；四道门禁全绿（29 套 / 248 项单测 100% 通过，生产构建全通）；遗留 4×P3 待 Round 2 闭环 |
| **2026-09-16** | `bb3be08` 独立审查 Round 1（不公开房间创建/加入交互解耦） | [`2026-09-16-workbuddy-code-review-unlisted-room-round1-handoff.md`](2026-09-16-workbuddy-code-review-unlisted-room-round1-handoff.md) | 逐文件核验 6 文件 diff（+198/−47）并追踪 `GameShell.tsx:580` 第二消费点；以 CDP 无头 Chrome 在 `en/zh/ar/ru × 390/641/1280` 共 8 组视口实测（DOM 顺序与几何顺序一致、无横向溢出、无重叠、RTL 镜像正常、加长文案在侧边栏注记正常换行）；独立复跑 `tsc`/`lint` 零问题、`vitest --pool=vmForks` 28 套 / 245 例全绿；0×P0/1/2，**4×P3**（`joinExistingRoom` 六语种死键且交接单虚述其已交付、`aria-hidden` 分隔带致区分语义整体移出无障碍树且 hint 无 `aria-describedby`、验收标准 1 零自动化守门——组件整体回退到 `165c56d` 后 245 例仍全绿、"private share link" 与同面板「这不是访问保护」语义相悖）；**未通过**，4 项已由 `800a3cb` 完成功能性修复（遗留缺陷见 Round 2 复查） |
| **2026-09-16** | 不公开房间创建与加入交互解耦及文案歧义消除 | [`2026-09-16-unlisted-room-creation-disambiguation-handoff.md`](2026-09-16-unlisted-room-creation-disambiguation-handoff.md) | 在线大厅“与好友游玩”拆分为创建不公开房间与加入已有房间两大独立卡片；6 语种字典同步扩充 `createUnlistedRoomHint`、`joinExistingRoom` 与 `orJoinExisting`；消除用户误以为输入框可用于自定义创建房间号的交互歧义；四道门禁全绿（245 项测试全通） |
| **2026-09-16** | Round 1 审查缺陷修复（P3-1 至 P3-5）与调度器接缝抽取 | [`2026-09-16-round1-findings-remediation-handoff.md`](2026-09-16-round1-findings-remediation-handoff.md) | 抽取 `createAiCountdownScheduler` 纯调度器接缝与可控时钟守门测试；`computeAiThinkingSeconds` 收敛定义域与非有限值；`AiGameView` 活区挂载固定 `aria-label` 消除读屏逐秒刷屏；`finally` 定时器清理对齐 `requestId` 归属守卫；按契约回填 `STATUS.md:11` 前驱交付；四道门禁全绿（245 项单测通过） |
| **2026-09-16** | `3a226f1` 独立审查 Round 1（AI 思考倒计时提示功能） | [`2026-09-16-workbuddy-code-review-round1-handoff.md`](2026-09-16-workbuddy-code-review-round1-handoff.md) | 逐文件核验 9 文件 diff（+209/−29），验收项 1/3/4/5 无偏差、RTL 静态核验无物理属性引入；独立复跑 `tsc`/`lint`/`build` 全绿、`vitest` 243/244（唯一失败为 `game-records.test.ts` Windows EPERM 环境 flake，单跑 10/10 通过），并以真实模块执行 15 组边界探针；0×P0/1/2，**5×P3**（`STATUS.md:11` 双字段未回填、逐秒跳动数值置于 `aria-live` 活区致读屏连续播报、`finally` 清理未按 `requestId` 守卫会误杀新请求定时器（UI 守卫遮蔽）、验收标准 2 零测试守门、`computeAiThinkingSeconds` 负 elapsed 返回超上限秒数）；**未通过、待修复复审** |
| **2026-09-16** | 人机对战 AI 思考倒计时提示功能交付 | [`2026-09-16-ai-thinking-countdown-handoff.md`](2026-09-16-ai-thinking-countdown-handoff.md) | 引入 `computeAiThinkingSeconds` 与 `aiThinkingCountdown` 秒级定时采样；6 语种字典同步更新 `thinkingCountdown`；侧边栏与棋盘上方 action-bar 双通道展示带有脉冲动画与 A11y 属性的倒计时指示器；四道门禁全绿（244 项单测通过，Next 生产构建成功） |
| **2026-09-15** | `655c667` Feedback 与日志采集计划审查（需求文档审查，源码零改动） | [`2026-09-15-feedback-plan-review-handoff.md`](2026-09-15-feedback-plan-review-handoff.md) | 先取回本仓实测基线（8 条带 `文件:行号` 的实测前提）再施审：**2×P0**（`.gitignore` 未覆盖 `data/feedback`+`data/runtime-logs` 且 `.jsonl` 绕过 `*.log` 规则；线上纯 HTTP 而计划以 HTTPS 为前提却未列为前置条件）、**6×P1**（同步工具目标/算法/验收三者互斥导致「只抓坏文件」能力不成立；保留期承诺无阶段承接者；验收不含隐私政策与 consent；图像处理与 multipart 解析零选型；未剥离 EXIF/GPS；Origin 同源基准未定义可被 `Host` 伪造绕过）、**13×P2** 与 **6×P3**。按用户决策记录「首版取消图片上传支持」，并分离出该决策**消解**与**未消解**的缺陷，图片缺陷提出完整保留 |
| **2026-09-13** | Round 7 审查缺陷修复（P3-1 至 P3-4）与规范索引闭环 | [`2026-09-13-round7-findings-remediation-handoff.md`](2026-09-13-round7-findings-remediation-handoff.md) | 纠正反代单级 XFF 论证，补齐 `$http_cf_connecting_ip` CDN 回源白名单前置条件；补充悔棋超时生命周期推进注释与测试守门（超期响应 `undo-request-missing` 及步进时钟原子性）；消除孤儿引用，在 AGENTS/README/INDEX 挂载工作流规范；四道门禁全绿（243 项测试通过） |
| **2026-09-13** | `948d239` 独立审查 Round 7（全量审计 P3 修复 + Push for Review 契约固化） | [`2026-09-13-workbuddy-code-review-round7-handoff.md`](2026-09-13-workbuddy-code-review-round7-handoff.md) | 逐文件核验 8 文件 diff；`IV-07` 孤儿引用确已清零、`room-state-machine.ts:798` 死分支确已移除；独立复跑 `tsc`/`lint`/`vitest`(28 套 242 项) 全绿并以可控时钟 + nginx XFF 语义仿真探针证伪；0×P0/1/2，**4×P3**（单级 XFF 论证自相矛盾、`$http_cf_connecting_ip` 缺来源白名单前置、超时分支移除非行为等价且零测试守门、新增规范文档孤儿零引用）；**未通过、待修复复审** |
| **2026-09-13** | 全量代码审查 (Full Codebase Audit Review) | [`2026-09-13-full-codebase-audit-review-handoff.md`](2026-09-13-full-codebase-audit-review-handoff.md) | 依据优化后去噪规范完成全仓审计；通过项 10 行内极简概括；0×P0/1/2，2×P3（P3-1 悔棋超时死分支，P3-2 反代 XFF 采信部署说明）；**已全部修复闭环** |
| **2026-09-13** | `ea0c0b9` 独立审查 Round 6（Phase 5 AI 引擎分层） | [`2026-09-13-workbuddy-code-review-round6-handoff.md`](2026-09-13-workbuddy-code-review-round6-handoff.md) | 声明级字节比对（base 134 声明 0 缺失 / 1 新增 / 2 等价变更）+ 36 项搜索差分 + 4500+ 项纯函数与配置边界差分证实零行为漂移；7/7 值导出零破坏 + 9 调用方零改动；四道门禁、Arena（含历史基线容错必要性反证）与 `verify:online` 复跑全绿；0×P0/1/2，1×P3（交付文档 4 项行数失真，**已在 `3d4a1a1` 校准闭环**） |
| **2026-09-13** | Phase 5 五子棋核心算法分层与 AI 引擎解耦 | [`2026-09-13-phase5-ai-engine-decomp-handoff.md`](2026-09-13-phase5-ai-engine-decomp-handoff.md) | `src/game/ai.ts`（2574 行）解耦为静态评估器 `ai-evaluator.ts`（564 行）、搜索引擎 `ai-search.ts`（1270 行）与策略调度器 `ai-scheduler.ts`（823 行），原文件蜕变为 27 行 Facade 门面，11 个公开方法与类型 100% 零破坏兼容，四道门禁全绿，Arena 天梯对战平局胜率稳定 |
| **2026-09-13** | `23b03fb` 独立审查 Round 5（Phase 4 服务端领域解耦） | [`2026-09-13-workbuddy-code-review-round5-handoff.md`](2026-09-13-workbuddy-code-review-round5-handoff.md) | 逐块核验 98 base 块字节等价（仅 14 处等价改写）、41 方法/31 类型零破坏、691 项差分断言 + 116 项版本契约断言零差异、四道门禁与三套烟测复跑全绿；0×P0/1/2，**1×P3**（`STATUS.md:11` 阶段交付提交第三次滞后）；**未通过、待修复复审** |
| **2026-09-13** | `23b03fb` Phase 4 服务端领域服务解耦 | [`2026-09-13-phase4-server-rooms-decomp-handoff.md`](2026-09-13-phase4-server-rooms-decomp-handoff.md) | `src/server/rooms.ts`（2414 行）解耦为 RoomStateMachine（2130 行）、PresenceTracker（280 行）、LeaderboardService（87 行）三大微领域服务，原文件蜕变为 324 行 Facade 门面，41 个方法与 31 个类型 100% 零破坏兼容，四道门禁+全套联机烟测全绿 |
| **2026-09-13** | `3e201ec` 独立审查 Round 4（Phase 3 修复收敛） | [`2026-09-13-workbuddy-code-review-round4-handoff.md`](2026-09-13-workbuddy-code-review-round4-handoff.md) | 核验侧栏 AI 思考中文案恢复、消除 6 语种孤儿键、精确校准 handoff 行数与权威提交；0×P0/1/2，1×P3（STATUS 提交双字段方案）；**审查通过**，Round 3 全部 4 项闭环 |
| **2026-09-13** | `b45e3fb` 独立审查 Round 3（Phase 3） | [`2026-09-13-workbuddy-code-review-round3-handoff.md`](2026-09-13-workbuddy-code-review-round3-handoff.md) | 逐文件核验 `useAiGame` 抽离等价性、`RoomContext` 双路径兼容与 6 面板 14 项选择器契约；0×P0/1，**1×P2**（AI 思考中文案被静默移除），3×P3（行数声明 +1、STATUS 提交号过期、i18n 孤儿键）；**未通过、待复审** |
| **2026-09-13** | Phase 3 联机大厅与表现层组件化 | [`2026-09-13-phase3-frontend-ui-decomp-handoff.md`](2026-09-13-phase3-frontend-ui-decomp-handoff.md) | `GameShell.tsx` 抽离 `useAiGame` Hook（瘦身 39.2%），新建 `RoomContext` 消除多层 Props 钻透，`OnlineLobbyView.tsx` 拆解为 6 个独立子面板，完整收敛 Phase 2 全部审查建议，四道门禁+联机烟测全绿 |
| **2026-09-13** | `785c8d4` 独立审查 Round 2（Phase 2） | [`2026-09-13-workbuddy-code-review-round2-handoff.md`](2026-09-13-workbuddy-code-review-round2-handoff.md) | 逐文件核验 4 子 Hook + 装配器等价性与 79 字段零破坏；0×P0/1/2，6×P3（STATUS 游离提交号、handoff 行数失真、`createRoom` 守卫弱化、`clearClosedRoom` 无条件清理、单测同义反复、陈旧注释） |
| **2026-09-13** | Phase 2 前端状态 Hook 解耦与代码卫生收敛 | [`2026-09-13-phase2-usefriendroom-decomp-handoff.md`](2026-09-13-phase2-usefriendroom-decomp-handoff.md) | `useFriendRoom.ts` 拆解为 4 个专注子 Hook，消除 R6 模块级全局快照，收敛全部 4 项 P3 审查建议，四道门禁+联机烟测全绿 |
| **2026-09-13** | `1e6ef36` 独立审查 Round 1（Phase 1） | [`2026-09-13-workbuddy-code-review-round1-handoff.md`](2026-09-13-workbuddy-code-review-round1-handoff.md) | 逐点位核验零取值漂移；0×P0/1/2，4×P3（`BOARD_SIZE` 双源、`WIN_STONE_COUNT` 未消费、单测有效性、文档偏差） |
| **2026-09-13** | Phase 1 全局常量中枢与代码卫生治理 | [`2026-09-13-phase1-code-hygiene-handoff.md`](2026-09-13-phase1-code-hygiene-handoff.md) | 提取 `constants.ts`、`ThemeScript.tsx`、消除魔法值、规范访客与自由五子棋规则 |
| **2026-09-13** | 全量技术债与代码卫生分阶段重构蓝图 | [`2026-09-13-comprehensive-refactoring-master-plan-handoff.md`](2026-09-13-comprehensive-refactoring-master-plan-handoff.md) | 制定涵盖 Nit 与四大巨石模块（rooms/useFriendRoom/GameShell/ai）的 5 阶段渐进解耦蓝图 |
| **2026-09-11** | `9505216` 全量审查修复与大厅实时汇总 | [`2026-09-11-full-review-and-lobby-summary-handoff.md`](2026-09-11-full-review-and-lobby-summary-handoff.md) | 修复焦点与Profile残留、AI Worker复用池、开局库四档分级、IX-07实时统计 |

---

## 规范与流程

1. **命名规范**：`YYYY-MM-DD-<feature-or-fix>-handoff.md`。
2. **结构要求**：
   - 交付目标与背景
   - 关键变更与落地内容（代码路径与设计要点）
   - 本地门禁验证数据（`tsc`、`lint`、`test`、`build`）
   - 遗留技术债与下一步建议
3. **沉淀闭环**：
   - 生成文档后，在此文件上方表格顶部追加新记录；
   - 同步更新根目录 [`STATUS.md`](../../STATUS.md) 的指标与近期里程碑；
   - 执行 Git 提交并推送远端。
4. **标准协同审查工作流规范**：
   - 遵循 [`docs/templates/DUAL_AGENT_REVIEW_WORKFLOW.md`](../templates/DUAL_AGENT_REVIEW_WORKFLOW.md) 的状态机生命周期、开发/审查/修复契约与提示词模板。
