时间编号：2026-06-25 00:25:00 -04:00
任务 ID：20260625-001746-stage0-redo-ui
代理角色：实现子代理
agent id：Codex
昵称：Vega
状态：完成
写入范围：src/app/**、src/components/**、src/i18n/**、src/lib/**、src/app/globals.css、next-env.d.ts、docs/subagents/20260625-001746-stage0-redo-ui-实现-Vega.md

## 任务目标

重做阶段 0 UI 基础设施：默认英语根入口、六语言游戏入口、字典化核心 UI、阿拉伯语 RTL 且棋盘不镜像、浅色/黑暗模式切换与 localStorage 持久化、主题 token 覆盖页面/棋盘/状态栏/广告占位。

## 实际完成

- 新增 `src/i18n/config.ts`，定义 `en`、`zh`、`fr`、`es`、`ru`、`ar`、默认英语和 RTL 语言。
- 新增 `src/i18n/dictionaries.ts`，核心游戏 UI、状态、棋盘 aria、广告占位和控件文案均通过六语言字典提供。
- 根路径 `/` 改为重定向 `/en`。
- 新增 `/[locale]` 页面，生成六语言静态入口，非法 locale 调用 `notFound()`。
- 新增 `DocumentLocaleSync`，客户端同步 `<html lang>` 与 `dir`；阿拉伯语页面外层 `dir="rtl"`。
- `GomokuBoard` 显式 `dir="ltr"` 且 `.gomoku-board` CSS 设置 `direction: ltr`，避免 RTL 镜像棋盘。
- 新增 `LocaleSwitcher`，语言切换写入 `localStorage`。
- 新增 `ThemeToggle` 与 `src/lib/theme.ts`，主题按钮在 light/dark 间切换，写入 `localStorage`，并更新 `html[data-theme]`。
- `src/app/layout.tsx` 加入首屏主题脚本，刷新后按 `localStorage` 或系统偏好设置 `data-theme`。
- 重写 `src/app/globals.css` 主题 token，覆盖页面背景、面板、棋盘、棋子、状态卡、广告占位、按钮和移动端布局。
- `next-env.d.ts` 被 `next build` 更新为生产类型路由引用：`.next/types/routes.d.ts`。

## 修改文件

- `src/app/page.tsx`
- `src/app/[locale]/page.tsx`
- `src/app/layout.tsx`
- `src/app/globals.css`
- `src/components/GameShell.tsx`
- `src/components/GomokuBoard.tsx`
- `src/components/DocumentLocaleSync.tsx`
- `src/components/LocaleSwitcher.tsx`
- `src/components/ThemeToggle.tsx`
- `src/i18n/config.ts`
- `src/i18n/dictionaries.ts`
- `src/lib/theme.ts`
- `next-env.d.ts`
- `docs/subagents/20260625-001746-stage0-redo-ui-实现-Vega.md`

## 验证结果

- `git diff --check`：通过。仅有 CRLF 提示，无 whitespace error。
- `npm run lint`：通过。
- `npm run build`：通过。构建输出显示 `/` 与 `/[locale]`，并预渲染 `/en`、`/zh`、`/fr`、另外三种 locale 路径。
- 文案扫描：`src/app`、`src/components`、`src/i18n`、`src/lib` 中中文 UI 命中只存在于 `src/i18n/dictionaries.ts` 的中文字典。

## 未覆盖项

- 未启动真实浏览器逐页点击验证主题刷新持久化；当前通过代码路径、lint 和 production build 验证。
- 未实现 SEO `generateMetadata` / `hreflang` / sitemap；本任务目标聚焦新版阶段 0 游戏入口 UI 基础设施。
- 工作区已有 `src/game/**` 和 rules 子代理报告改动，不属于本任务写入范围，本实现未修改和未回滚。

## 交给主控或下一位子代理的建议

- 独立验证时建议用浏览器覆盖 `/`、`/en`、`/zh`、`/fr`、`/es`、`/ru`、`/ar`，重点检查 Arabic RTL、棋盘 LTR、刷新后主题保持。
- 后续 SEO 子任务可基于现有 `locales` 和 `dictionaries` 扩展 metadata、alternates 与 sitemap。
